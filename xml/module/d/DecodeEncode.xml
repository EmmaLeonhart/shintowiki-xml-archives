<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:DecodeEncode</title>
    <ns>828</ns>
    <id>431</id>
    <revision>
      <id>490531</id>
      <timestamp>2023-04-17T20:18:54Z</timestamp>
      <contributor>
        <username>Lemondoge</username>
        <id>131</id>
      </contributor>
      <comment>Fixed error (`a ~= (nil or '')` doesn't work; change to `a and a ~= ''`).</comment>
      <origin>490531</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1670" sha1="i6pnw8l3pyqcqxofzxz6auze5i1q3g8" xml:space="preserve">require('strict')
local p = {}

local function _getBoolean( boolean_str )
	-- from: module:String; adapted
	-- requires an explicit true
	local boolean_value

	if type( boolean_str ) == 'string' then
		boolean_str = boolean_str:lower()
		if boolean_str == 'true' or boolean_str == 'yes' or boolean_str == '1' then
			boolean_value = true
		else
			boolean_value = false
		end
	elseif type( boolean_str ) == 'boolean' then
		boolean_value = boolean_str
	else
		boolean_value = false
	end
	return boolean_value
end

function p.decode( frame )
	local s = frame.args['s'] or ''
	local subset_only = _getBoolean(frame.args['subset_only'] or false)

	return p._decode( s, subset_only )
end

function p._decode( s, subset_only )
	-- U+2009 THIN SPACE: workaround for bug: HTML entity &amp;thinsp; is decoded incorrect. Entity &amp;ThinSpace; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;thinsp;', '&amp;ThinSpace;' )
	-- U+03B5 ε GREEK SMALL LETTER EPSILON: workaround for bug (phab:T328840): HTML entity &amp;epsilon; is decoded incorrect for gsub(). Entity &amp;epsi; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;epsilon;', '&amp;epsi;' )

	local ret = mw.text.decode( s, not subset_only )

	return ret
end

function p.encode( frame )
	local s = frame.args['s'] or ''
	local charset = frame.args['charset']

	return p._encode( s, charset )
end

function p._encode( s, charset )
	-- example: charset = '_&amp;©−°\\\"\'\=' -- do escape with backslash not %;
	local ret

	if charset and charset ~= '' then
		ret = mw.text.encode( s, charset )
	else
		-- use default: chartset = '&lt;&gt;&amp;"\' ' (outer quotes = lua required; space = NBSP)
		ret = mw.text.encode( s )
	end 
	
	return ret
end

return p</text>
      <sha1>i6pnw8l3pyqcqxofzxz6auze5i1q3g8</sha1>
    </revision>
    <revision>
      <id>490532</id>
      <parentid>490531</parentid>
      <timestamp>2024-06-25T16:09:43Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:wikipedia:Module:DecodeEncode]]: importing template</comment>
      <origin>490531</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1670" sha1="i6pnw8l3pyqcqxofzxz6auze5i1q3g8" xml:space="preserve">require('strict')
local p = {}

local function _getBoolean( boolean_str )
	-- from: module:String; adapted
	-- requires an explicit true
	local boolean_value

	if type( boolean_str ) == 'string' then
		boolean_str = boolean_str:lower()
		if boolean_str == 'true' or boolean_str == 'yes' or boolean_str == '1' then
			boolean_value = true
		else
			boolean_value = false
		end
	elseif type( boolean_str ) == 'boolean' then
		boolean_value = boolean_str
	else
		boolean_value = false
	end
	return boolean_value
end

function p.decode( frame )
	local s = frame.args['s'] or ''
	local subset_only = _getBoolean(frame.args['subset_only'] or false)

	return p._decode( s, subset_only )
end

function p._decode( s, subset_only )
	-- U+2009 THIN SPACE: workaround for bug: HTML entity &amp;thinsp; is decoded incorrect. Entity &amp;ThinSpace; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;thinsp;', '&amp;ThinSpace;' )
	-- U+03B5 ε GREEK SMALL LETTER EPSILON: workaround for bug (phab:T328840): HTML entity &amp;epsilon; is decoded incorrect for gsub(). Entity &amp;epsi; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;epsilon;', '&amp;epsi;' )

	local ret = mw.text.decode( s, not subset_only )

	return ret
end

function p.encode( frame )
	local s = frame.args['s'] or ''
	local charset = frame.args['charset']

	return p._encode( s, charset )
end

function p._encode( s, charset )
	-- example: charset = '_&amp;©−°\\\"\'\=' -- do escape with backslash not %;
	local ret

	if charset and charset ~= '' then
		ret = mw.text.encode( s, charset )
	else
		-- use default: chartset = '&lt;&gt;&amp;"\' ' (outer quotes = lua required; space = NBSP)
		ret = mw.text.encode( s )
	end 
	
	return ret
end

return p</text>
      <sha1>i6pnw8l3pyqcqxofzxz6auze5i1q3g8</sha1>
    </revision>
    <revision>
      <id>490533</id>
      <parentid>490532</parentid>
      <timestamp>2025-03-06T08:37:13Z</timestamp>
      <contributor>
        <username>Lemondoge</username>
        <id>131</id>
      </contributor>
      <comment>Fixed error (`a ~= (nil or '')` doesn't work; change to `a and a ~= ''`).</comment>
      <origin>490531</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1670" sha1="i6pnw8l3pyqcqxofzxz6auze5i1q3g8" xml:space="preserve">require('strict')
local p = {}

local function _getBoolean( boolean_str )
	-- from: module:String; adapted
	-- requires an explicit true
	local boolean_value

	if type( boolean_str ) == 'string' then
		boolean_str = boolean_str:lower()
		if boolean_str == 'true' or boolean_str == 'yes' or boolean_str == '1' then
			boolean_value = true
		else
			boolean_value = false
		end
	elseif type( boolean_str ) == 'boolean' then
		boolean_value = boolean_str
	else
		boolean_value = false
	end
	return boolean_value
end

function p.decode( frame )
	local s = frame.args['s'] or ''
	local subset_only = _getBoolean(frame.args['subset_only'] or false)

	return p._decode( s, subset_only )
end

function p._decode( s, subset_only )
	-- U+2009 THIN SPACE: workaround for bug: HTML entity &amp;thinsp; is decoded incorrect. Entity &amp;ThinSpace; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;thinsp;', '&amp;ThinSpace;' )
	-- U+03B5 ε GREEK SMALL LETTER EPSILON: workaround for bug (phab:T328840): HTML entity &amp;epsilon; is decoded incorrect for gsub(). Entity &amp;epsi; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;epsilon;', '&amp;epsi;' )

	local ret = mw.text.decode( s, not subset_only )

	return ret
end

function p.encode( frame )
	local s = frame.args['s'] or ''
	local charset = frame.args['charset']

	return p._encode( s, charset )
end

function p._encode( s, charset )
	-- example: charset = '_&amp;©−°\\\"\'\=' -- do escape with backslash not %;
	local ret

	if charset and charset ~= '' then
		ret = mw.text.encode( s, charset )
	else
		-- use default: chartset = '&lt;&gt;&amp;"\' ' (outer quotes = lua required; space = NBSP)
		ret = mw.text.encode( s )
	end 
	
	return ret
end

return p</text>
      <sha1>i6pnw8l3pyqcqxofzxz6auze5i1q3g8</sha1>
    </revision>
    <revision>
      <id>490534</id>
      <parentid>490533</parentid>
      <timestamp>2025-03-06T08:37:14Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing the refn stuff to try to fix the Okinawa article</comment>
      <origin>490531</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1670" sha1="i6pnw8l3pyqcqxofzxz6auze5i1q3g8" xml:space="preserve">require('strict')
local p = {}

local function _getBoolean( boolean_str )
	-- from: module:String; adapted
	-- requires an explicit true
	local boolean_value

	if type( boolean_str ) == 'string' then
		boolean_str = boolean_str:lower()
		if boolean_str == 'true' or boolean_str == 'yes' or boolean_str == '1' then
			boolean_value = true
		else
			boolean_value = false
		end
	elseif type( boolean_str ) == 'boolean' then
		boolean_value = boolean_str
	else
		boolean_value = false
	end
	return boolean_value
end

function p.decode( frame )
	local s = frame.args['s'] or ''
	local subset_only = _getBoolean(frame.args['subset_only'] or false)

	return p._decode( s, subset_only )
end

function p._decode( s, subset_only )
	-- U+2009 THIN SPACE: workaround for bug: HTML entity &amp;thinsp; is decoded incorrect. Entity &amp;ThinSpace; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;thinsp;', '&amp;ThinSpace;' )
	-- U+03B5 ε GREEK SMALL LETTER EPSILON: workaround for bug (phab:T328840): HTML entity &amp;epsilon; is decoded incorrect for gsub(). Entity &amp;epsi; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;epsilon;', '&amp;epsi;' )

	local ret = mw.text.decode( s, not subset_only )

	return ret
end

function p.encode( frame )
	local s = frame.args['s'] or ''
	local charset = frame.args['charset']

	return p._encode( s, charset )
end

function p._encode( s, charset )
	-- example: charset = '_&amp;©−°\\\"\'\=' -- do escape with backslash not %;
	local ret

	if charset and charset ~= '' then
		ret = mw.text.encode( s, charset )
	else
		-- use default: chartset = '&lt;&gt;&amp;"\' ' (outer quotes = lua required; space = NBSP)
		ret = mw.text.encode( s )
	end 
	
	return ret
end

return p</text>
      <sha1>i6pnw8l3pyqcqxofzxz6auze5i1q3g8</sha1>
    </revision>
    <revision>
      <id>490535</id>
      <parentid>490534</parentid>
      <timestamp>2025-03-06T08:38:23Z</timestamp>
      <contributor>
        <username>Lemondoge</username>
        <id>131</id>
      </contributor>
      <comment>Fixed error (`a ~= (nil or '')` doesn't work; change to `a and a ~= ''`).</comment>
      <origin>490531</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1670" sha1="i6pnw8l3pyqcqxofzxz6auze5i1q3g8" xml:space="preserve">require('strict')
local p = {}

local function _getBoolean( boolean_str )
	-- from: module:String; adapted
	-- requires an explicit true
	local boolean_value

	if type( boolean_str ) == 'string' then
		boolean_str = boolean_str:lower()
		if boolean_str == 'true' or boolean_str == 'yes' or boolean_str == '1' then
			boolean_value = true
		else
			boolean_value = false
		end
	elseif type( boolean_str ) == 'boolean' then
		boolean_value = boolean_str
	else
		boolean_value = false
	end
	return boolean_value
end

function p.decode( frame )
	local s = frame.args['s'] or ''
	local subset_only = _getBoolean(frame.args['subset_only'] or false)

	return p._decode( s, subset_only )
end

function p._decode( s, subset_only )
	-- U+2009 THIN SPACE: workaround for bug: HTML entity &amp;thinsp; is decoded incorrect. Entity &amp;ThinSpace; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;thinsp;', '&amp;ThinSpace;' )
	-- U+03B5 ε GREEK SMALL LETTER EPSILON: workaround for bug (phab:T328840): HTML entity &amp;epsilon; is decoded incorrect for gsub(). Entity &amp;epsi; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;epsilon;', '&amp;epsi;' )

	local ret = mw.text.decode( s, not subset_only )

	return ret
end

function p.encode( frame )
	local s = frame.args['s'] or ''
	local charset = frame.args['charset']

	return p._encode( s, charset )
end

function p._encode( s, charset )
	-- example: charset = '_&amp;©−°\\\"\'\=' -- do escape with backslash not %;
	local ret

	if charset and charset ~= '' then
		ret = mw.text.encode( s, charset )
	else
		-- use default: chartset = '&lt;&gt;&amp;"\' ' (outer quotes = lua required; space = NBSP)
		ret = mw.text.encode( s )
	end 
	
	return ret
end

return p</text>
      <sha1>i6pnw8l3pyqcqxofzxz6auze5i1q3g8</sha1>
    </revision>
    <revision>
      <id>490536</id>
      <parentid>490535</parentid>
      <timestamp>2025-03-06T08:38:24Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing the refn stuff to try to fix the Okinawa article</comment>
      <origin>490531</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1670" sha1="i6pnw8l3pyqcqxofzxz6auze5i1q3g8" xml:space="preserve">require('strict')
local p = {}

local function _getBoolean( boolean_str )
	-- from: module:String; adapted
	-- requires an explicit true
	local boolean_value

	if type( boolean_str ) == 'string' then
		boolean_str = boolean_str:lower()
		if boolean_str == 'true' or boolean_str == 'yes' or boolean_str == '1' then
			boolean_value = true
		else
			boolean_value = false
		end
	elseif type( boolean_str ) == 'boolean' then
		boolean_value = boolean_str
	else
		boolean_value = false
	end
	return boolean_value
end

function p.decode( frame )
	local s = frame.args['s'] or ''
	local subset_only = _getBoolean(frame.args['subset_only'] or false)

	return p._decode( s, subset_only )
end

function p._decode( s, subset_only )
	-- U+2009 THIN SPACE: workaround for bug: HTML entity &amp;thinsp; is decoded incorrect. Entity &amp;ThinSpace; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;thinsp;', '&amp;ThinSpace;' )
	-- U+03B5 ε GREEK SMALL LETTER EPSILON: workaround for bug (phab:T328840): HTML entity &amp;epsilon; is decoded incorrect for gsub(). Entity &amp;epsi; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;epsilon;', '&amp;epsi;' )

	local ret = mw.text.decode( s, not subset_only )

	return ret
end

function p.encode( frame )
	local s = frame.args['s'] or ''
	local charset = frame.args['charset']

	return p._encode( s, charset )
end

function p._encode( s, charset )
	-- example: charset = '_&amp;©−°\\\"\'\=' -- do escape with backslash not %;
	local ret

	if charset and charset ~= '' then
		ret = mw.text.encode( s, charset )
	else
		-- use default: chartset = '&lt;&gt;&amp;"\' ' (outer quotes = lua required; space = NBSP)
		ret = mw.text.encode( s )
	end 
	
	return ret
end

return p</text>
      <sha1>i6pnw8l3pyqcqxofzxz6auze5i1q3g8</sha1>
    </revision>
    <revision>
      <id>490537</id>
      <parentid>490536</parentid>
      <timestamp>2025-11-21T00:34:50Z</timestamp>
      <contributor>
        <username>en&gt;Djsasso</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>1 revision imported from [[:en:Module:DecodeEncode]]</comment>
      <origin>490531</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1670" sha1="i6pnw8l3pyqcqxofzxz6auze5i1q3g8" xml:space="preserve">require('strict')
local p = {}

local function _getBoolean( boolean_str )
	-- from: module:String; adapted
	-- requires an explicit true
	local boolean_value

	if type( boolean_str ) == 'string' then
		boolean_str = boolean_str:lower()
		if boolean_str == 'true' or boolean_str == 'yes' or boolean_str == '1' then
			boolean_value = true
		else
			boolean_value = false
		end
	elseif type( boolean_str ) == 'boolean' then
		boolean_value = boolean_str
	else
		boolean_value = false
	end
	return boolean_value
end

function p.decode( frame )
	local s = frame.args['s'] or ''
	local subset_only = _getBoolean(frame.args['subset_only'] or false)

	return p._decode( s, subset_only )
end

function p._decode( s, subset_only )
	-- U+2009 THIN SPACE: workaround for bug: HTML entity &amp;thinsp; is decoded incorrect. Entity &amp;ThinSpace; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;thinsp;', '&amp;ThinSpace;' )
	-- U+03B5 ε GREEK SMALL LETTER EPSILON: workaround for bug (phab:T328840): HTML entity &amp;epsilon; is decoded incorrect for gsub(). Entity &amp;epsi; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;epsilon;', '&amp;epsi;' )

	local ret = mw.text.decode( s, not subset_only )

	return ret
end

function p.encode( frame )
	local s = frame.args['s'] or ''
	local charset = frame.args['charset']

	return p._encode( s, charset )
end

function p._encode( s, charset )
	-- example: charset = '_&amp;©−°\\\"\'\=' -- do escape with backslash not %;
	local ret

	if charset and charset ~= '' then
		ret = mw.text.encode( s, charset )
	else
		-- use default: chartset = '&lt;&gt;&amp;"\' ' (outer quotes = lua required; space = NBSP)
		ret = mw.text.encode( s )
	end 
	
	return ret
end

return p</text>
      <sha1>i6pnw8l3pyqcqxofzxz6auze5i1q3g8</sha1>
    </revision>
    <revision>
      <id>490538</id>
      <parentid>490537</parentid>
      <timestamp>2025-11-21T00:34:51Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: overwriting coordinates so I hope they start working</comment>
      <origin>490531</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1670" sha1="i6pnw8l3pyqcqxofzxz6auze5i1q3g8" xml:space="preserve">require('strict')
local p = {}

local function _getBoolean( boolean_str )
	-- from: module:String; adapted
	-- requires an explicit true
	local boolean_value

	if type( boolean_str ) == 'string' then
		boolean_str = boolean_str:lower()
		if boolean_str == 'true' or boolean_str == 'yes' or boolean_str == '1' then
			boolean_value = true
		else
			boolean_value = false
		end
	elseif type( boolean_str ) == 'boolean' then
		boolean_value = boolean_str
	else
		boolean_value = false
	end
	return boolean_value
end

function p.decode( frame )
	local s = frame.args['s'] or ''
	local subset_only = _getBoolean(frame.args['subset_only'] or false)

	return p._decode( s, subset_only )
end

function p._decode( s, subset_only )
	-- U+2009 THIN SPACE: workaround for bug: HTML entity &amp;thinsp; is decoded incorrect. Entity &amp;ThinSpace; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;thinsp;', '&amp;ThinSpace;' )
	-- U+03B5 ε GREEK SMALL LETTER EPSILON: workaround for bug (phab:T328840): HTML entity &amp;epsilon; is decoded incorrect for gsub(). Entity &amp;epsi; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;epsilon;', '&amp;epsi;' )

	local ret = mw.text.decode( s, not subset_only )

	return ret
end

function p.encode( frame )
	local s = frame.args['s'] or ''
	local charset = frame.args['charset']

	return p._encode( s, charset )
end

function p._encode( s, charset )
	-- example: charset = '_&amp;©−°\\\"\'\=' -- do escape with backslash not %;
	local ret

	if charset and charset ~= '' then
		ret = mw.text.encode( s, charset )
	else
		-- use default: chartset = '&lt;&gt;&amp;"\' ' (outer quotes = lua required; space = NBSP)
		ret = mw.text.encode( s )
	end 
	
	return ret
end

return p</text>
      <sha1>i6pnw8l3pyqcqxofzxz6auze5i1q3g8</sha1>
    </revision>
    <revision>
      <id>432</id>
      <timestamp>2025-11-21T00:36:11Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was 1 revision imported: overwriting coordinates so I hope they start working</comment>
      <origin>432</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1670" sha1="i6pnw8l3pyqcqxofzxz6auze5i1q3g8" xml:space="preserve">require('strict')
local p = {}

local function _getBoolean( boolean_str )
	-- from: module:String; adapted
	-- requires an explicit true
	local boolean_value

	if type( boolean_str ) == 'string' then
		boolean_str = boolean_str:lower()
		if boolean_str == 'true' or boolean_str == 'yes' or boolean_str == '1' then
			boolean_value = true
		else
			boolean_value = false
		end
	elseif type( boolean_str ) == 'boolean' then
		boolean_value = boolean_str
	else
		boolean_value = false
	end
	return boolean_value
end

function p.decode( frame )
	local s = frame.args['s'] or ''
	local subset_only = _getBoolean(frame.args['subset_only'] or false)

	return p._decode( s, subset_only )
end

function p._decode( s, subset_only )
	-- U+2009 THIN SPACE: workaround for bug: HTML entity &amp;thinsp; is decoded incorrect. Entity &amp;ThinSpace; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;thinsp;', '&amp;ThinSpace;' )
	-- U+03B5 ε GREEK SMALL LETTER EPSILON: workaround for bug (phab:T328840): HTML entity &amp;epsilon; is decoded incorrect for gsub(). Entity &amp;epsi; gets decoded properly
	s = mw.ustring.gsub( s, '&amp;epsilon;', '&amp;epsi;' )

	local ret = mw.text.decode( s, not subset_only )

	return ret
end

function p.encode( frame )
	local s = frame.args['s'] or ''
	local charset = frame.args['charset']

	return p._encode( s, charset )
end

function p._encode( s, charset )
	-- example: charset = '_&amp;©−°\\\"\'\=' -- do escape with backslash not %;
	local ret

	if charset and charset ~= '' then
		ret = mw.text.encode( s, charset )
	else
		-- use default: chartset = '&lt;&gt;&amp;"\' ' (outer quotes = lua required; space = NBSP)
		ret = mw.text.encode( s )
	end 
	
	return ret
end

return p</text>
      <sha1>i6pnw8l3pyqcqxofzxz6auze5i1q3g8</sha1>
    </revision>
  </page>
</mediawiki>
