<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Help:Conditional expressions</title>
    <ns>12</ns>
    <id>2544</id>
    <revision>
      <id>677656</id>
      <timestamp>2024-12-02T15:11:16Z</timestamp>
      <contributor>
        <username>en&gt;P199</username>
        <id>0</id>
      </contributor>
      <comment>add example</comment>
      <origin>677656</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="27877" sha1="asfl9k5hse1t2l2pji4ipzeauh7c248" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]</text>
      <sha1>asfl9k5hse1t2l2pji4ipzeauh7c248</sha1>
    </revision>
    <revision>
      <id>677657</id>
      <parentid>677656</parentid>
      <timestamp>2024-12-07T03:35:40Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>1 revision imported: importing wikipedia</comment>
      <origin>677656</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="27877" sha1="asfl9k5hse1t2l2pji4ipzeauh7c248" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]</text>
      <sha1>asfl9k5hse1t2l2pji4ipzeauh7c248</sha1>
    </revision>
    <revision>
      <id>677658</id>
      <parentid>677657</parentid>
      <timestamp>2024-12-20T01:55:09Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Adding [[Category:pages from wikipedia]][[en:{{subst:PAGENAME}}]][[Category:pages from wikipedia]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677658</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="27974" sha1="7tj4p6678th64731xrxra0makopnum7" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:pages from wikipedia]][[en:Conditional expressions]][[Category:pages from wikipedia]]</text>
      <sha1>7tj4p6678th64731xrxra0makopnum7</sha1>
    </revision>
    <revision>
      <id>677659</id>
      <parentid>677658</parentid>
      <timestamp>2024-12-20T09:44:20Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Moving from [[Category:Pages from wikipedia]] to [[Category:Pages and Categories from wikipedia]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677659</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28005" sha1="rcx7a263p9opvxuqjkkxjly0euwcptg" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia]]
[[en:Conditional expressions]][[Category:Pages and Categories from wikipedia]]</text>
      <sha1>rcx7a263p9opvxuqjkkxjly0euwcptg</sha1>
    </revision>
    <revision>
      <id>677660</id>
      <parentid>677659</parentid>
      <timestamp>2024-12-20T10:00:38Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Removing from [[Category:Pages and Categories from wikipedia]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677660</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="27908" sha1="t53vfpudoczz12m5f2lupdou7k8yxmp" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[en:Conditional expressions]]</text>
      <sha1>t53vfpudoczz12m5f2lupdou7k8yxmp</sha1>
    </revision>
    <revision>
      <id>677661</id>
      <parentid>677660</parentid>
      <timestamp>2024-12-20T10:06:30Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from wikipedia]] to [[Category:Pages and Categories from wikipedia2]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677661</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="27958" sha1="iec3hge0zyg7vgstl1v0ojcbm5a02ov" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia2]]
[[en:Conditional expressions]]</text>
      <sha1>iec3hge0zyg7vgstl1v0ojcbm5a02ov</sha1>
    </revision>
    <revision>
      <id>677662</id>
      <parentid>677661</parentid>
      <timestamp>2024-12-20T11:22:06Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Moving from [[Category:Pages and Categories from wikipedia2]] to [[Category:Pages and Categories from wikipedia8]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677662</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="27958" sha1="gsnv44thr58mog7t8cx3huf0lqpkeyv" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia8]]
[[en:Conditional expressions]]</text>
      <sha1>gsnv44thr58mog7t8cx3huf0lqpkeyv</sha1>
    </revision>
    <revision>
      <id>677663</id>
      <parentid>677662</parentid>
      <timestamp>2024-12-28T12:14:14Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from wikipedia8]] to [[Category:pages from wikipedia]][[en:{{subst:PAGENAME}}]][[Category:pages from wikipedia]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677663</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28055" sha1="a0h59i32vupqai57xreg88ukpg664hs" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia8]]
[[Category:pages from wikipedia]][[en:Conditional expressions]][[Category:pages from wikipedia]]
[[en:Conditional expressions]]</text>
      <sha1>a0h59i32vupqai57xreg88ukpg664hs</sha1>
    </revision>
    <revision>
      <id>677664</id>
      <parentid>677663</parentid>
      <timestamp>2024-12-31T01:24:28Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Moving from [[Category:Pages from wikipedia]] to [[Category:Pages and Categories from wikipedia13]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677664</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28090" sha1="hl9hmz7xu5qwyqs1v6e8v162gqpgubz" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia8]]
[[Category:Pages and Categories from wikipedia13]]
[[en:Conditional expressions]][[Category:Pages and Categories from wikipedia13]]
[[en:Conditional expressions]]</text>
      <sha1>hl9hmz7xu5qwyqs1v6e8v162gqpgubz</sha1>
    </revision>
    <revision>
      <id>677665</id>
      <parentid>677664</parentid>
      <timestamp>2024-12-31T22:13:05Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Moving from [[Category:Pages and Categories from wikipedia8]] to [[Category:Pages and Categories from wikipedia08]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677665</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28091" sha1="trbre1nkq2nm8wgqbchz3gh930uzbuv" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia08]]
[[Category:Pages and Categories from wikipedia13]]
[[en:Conditional expressions]][[Category:Pages and Categories from wikipedia13]]
[[en:Conditional expressions]]</text>
      <sha1>trbre1nkq2nm8wgqbchz3gh930uzbuv</sha1>
    </revision>
    <revision>
      <id>677666</id>
      <parentid>677665</parentid>
      <timestamp>2025-01-02T04:58:37Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from wikipedia13]] to [[Category:dd]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677666</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28123" sha1="a3og1sgp3fws4qdpzs4v2xdxsqvzry8" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia08]]
[[Category:Pages and Categories from wikipedia13]]
[[Category:dd]]
[[en:Conditional expressions]][[Category:Pages and Categories from wikipedia13]]
[[Category:dd]]
[[en:Conditional expressions]]</text>
      <sha1>a3og1sgp3fws4qdpzs4v2xdxsqvzry8</sha1>
    </revision>
    <revision>
      <id>677667</id>
      <parentid>677666</parentid>
      <timestamp>2025-01-03T08:09:59Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Removing from [[Category:Dd]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677667</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28091" sha1="trbre1nkq2nm8wgqbchz3gh930uzbuv" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia08]]
[[Category:Pages and Categories from wikipedia13]]
[[en:Conditional expressions]][[Category:Pages and Categories from wikipedia13]]
[[en:Conditional expressions]]</text>
      <sha1>trbre1nkq2nm8wgqbchz3gh930uzbuv</sha1>
    </revision>
    <revision>
      <id>677668</id>
      <parentid>677667</parentid>
      <timestamp>2025-01-04T08:05:12Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from wikipedia08]] to [[Category:dd]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677668</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28107" sha1="ro6qm7jepbmmcp2mi9av1v2sjnyvrzm" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia08]]
[[Category:dd]]
[[Category:Pages and Categories from wikipedia13]]
[[en:Conditional expressions]][[Category:Pages and Categories from wikipedia13]]
[[en:Conditional expressions]]</text>
      <sha1>ro6qm7jepbmmcp2mi9av1v2sjnyvrzm</sha1>
    </revision>
    <revision>
      <id>677669</id>
      <parentid>677668</parentid>
      <timestamp>2025-01-04T08:06:11Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from wikipedia13]] to [[Category:dd]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677669</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28123" sha1="a3og1sgp3fws4qdpzs4v2xdxsqvzry8" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia08]]
[[Category:Pages and Categories from wikipedia13]]
[[Category:dd]]
[[en:Conditional expressions]][[Category:Pages and Categories from wikipedia13]]
[[Category:dd]]
[[en:Conditional expressions]]</text>
      <sha1>a3og1sgp3fws4qdpzs4v2xdxsqvzry8</sha1>
    </revision>
    <revision>
      <id>677670</id>
      <parentid>677669</parentid>
      <timestamp>2025-01-04T08:23:13Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Copying from [[Category:Pages and Categories from wikipedia08]] to [[Category:Top level categories]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677670</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28157" sha1="f3auzoqcg9mwg3avk05keofatonxwwk" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia08]]
[[Category:Top level categories]]
[[Category:Pages and Categories from wikipedia13]]
[[Category:dd]]
[[en:Conditional expressions]][[Category:Pages and Categories from wikipedia13]]
[[Category:dd]]
[[en:Conditional expressions]]</text>
      <sha1>f3auzoqcg9mwg3avk05keofatonxwwk</sha1>
    </revision>
    <revision>
      <id>677671</id>
      <parentid>677670</parentid>
      <timestamp>2025-01-05T10:50:45Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>removing these two bullshit categories</comment>
      <origin>677671</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28127" sha1="fnpcba0l2bni1mr152fvgp73jsofh2n" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia08]]
[[Category:Top level categories]]
[[Category:Pages and Categories from wikipedia13]]

[[en:Conditional expressions]][[Category:Pages and Categories from wikipedia13]]

[[en:Conditional expressions]]</text>
      <sha1>fnpcba0l2bni1mr152fvgp73jsofh2n</sha1>
    </revision>
    <revision>
      <id>677672</id>
      <parentid>677671</parentid>
      <timestamp>2025-01-06T11:56:27Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677672</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28121" sha1="mxt89xv14tap7aip5ptocyww9gfd1l7" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia]]
[[Category:Top level categories]]
[[Category:Pages and Categories from wikipedia]]

[[en:Conditional expressions]][[Category:Pages and Categories from wikipedia]]

[[en:Conditional expressions]]</text>
      <sha1>mxt89xv14tap7aip5ptocyww9gfd1l7</sha1>
    </revision>
    <revision>
      <id>677673</id>
      <parentid>677672</parentid>
      <timestamp>2025-01-06T12:01:55Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Moving from [[Category:Top level categories]] to [[Category:pages from wikipedia11]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677673</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28123" sha1="lhebas5nbe5ikmttcdbzuimrh0irxtz" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from wikipedia]]
[[Category:pages from wikipedia11]]
[[Category:Pages and Categories from wikipedia]]

[[en:Conditional expressions]][[Category:Pages and Categories from wikipedia]]

[[en:Conditional expressions]]</text>
      <sha1>lhebas5nbe5ikmttcdbzuimrh0irxtz</sha1>
    </revision>
    <revision>
      <id>677674</id>
      <parentid>677673</parentid>
      <timestamp>2025-01-07T06:50:10Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>putting category to lowercase</comment>
      <origin>677674</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28123" sha1="pyrh8rfaut53fb8odb9g66vm95gqk1n" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:WantedPages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[category:MediaWiki Help|Conditional Expressions]]
[[category:Wikipedia template help|Conditional Expressions]]
[[category:Pages and categories from wikipedia]]
[[category:pages from wikipedia11]]
[[category:Pages and categories from wikipedia]]

[[en:Conditional expressions]][[category:Pages and categories from wikipedia]]

[[en:Conditional expressions]]</text>
      <sha1>pyrh8rfaut53fb8odb9g66vm95gqk1n</sha1>
    </revision>
    <revision>
      <id>677675</id>
      <parentid>677674</parentid>
      <timestamp>2025-01-10T08:14:48Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>bumping</comment>
      <origin>677675</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28123" sha1="6x27rqferp91wgsmgx5we3nib3ln17s" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[category:MediaWiki Help|Conditional Expressions]]
[[category:Wikipedia template help|Conditional Expressions]]
[[category:pages and categories from Wikipedia]]
[[category:pages from Wikipedia11]]
[[category:pages and categories from Wikipedia]]

[[en:Conditional expressions]][[category:pages and categories from Wikipedia]]

[[en:Conditional expressions]]</text>
      <sha1>6x27rqferp91wgsmgx5we3nib3ln17s</sha1>
    </revision>
    <revision>
      <id>677676</id>
      <parentid>677675</parentid>
      <timestamp>2025-01-10T08:43:05Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Copying from [[Category:Pages and Categories from wikipedia]] to [[Category:kk]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677676</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28139" sha1="flkx2senfolzjhzv6l8m0zyiaoq1y5k" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[category:MediaWiki Help|Conditional Expressions]]
[[category:Wikipedia template help|Conditional Expressions]]
[[category:pages and categories from Wikipedia]]
[[category:pages from Wikipedia11]]
[[category:pages and categories from Wikipedia]]

[[en:Conditional expressions]][[category:pages and categories from Wikipedia]]
[[Category:kk]]

[[en:Conditional expressions]]</text>
      <sha1>flkx2senfolzjhzv6l8m0zyiaoq1y5k</sha1>
    </revision>
    <revision>
      <id>677677</id>
      <parentid>677676</parentid>
      <timestamp>2025-01-13T05:20:37Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>dummy edit to bring up all the wikipedia articles to the top</comment>
      <origin>677677</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28139" sha1="kjkpk7qr9df0ve92wba41fdzoismc6d" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:pages and categories from Wikipedia]]
[[Category:pages from Wikipedia11]]
[[Category:pages and categories from Wikipedia]]

[[en:Conditional expressions]][[Category:pages and categories from Wikipedia]]
[[Category:kk]]

[[en:Conditional expressions]]</text>
      <sha1>kjkpk7qr9df0ve92wba41fdzoismc6d</sha1>
    </revision>
    <revision>
      <id>677678</id>
      <parentid>677677</parentid>
      <timestamp>2025-01-13T06:15:13Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Removing from [[Category:Kk]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677678</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28122" sha1="0vj2l6clctvxjvvav1i6nary0sd49t9" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:pages and categories from Wikipedia]]
[[Category:pages from Wikipedia11]]
[[Category:pages and categories from Wikipedia]]

[[en:Conditional expressions]][[Category:pages and categories from Wikipedia]]
[[en:Conditional expressions]]</text>
      <sha1>0vj2l6clctvxjvvav1i6nary0sd49t9</sha1>
    </revision>
    <revision>
      <id>677679</id>
      <parentid>677678</parentid>
      <timestamp>2025-01-16T09:25:44Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and categories from Wikipedia]] to [[Category:kk]][[en:{{subst:FULLPAGENAME}}]][[Category:kk]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677679</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28319" sha1="e89dhqhsckqe7e1ll1zk4ysp0p3yy6p" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and categories from Wikipedia]]
[[Category:kk]][[en:Help:Conditional expressions]][[Category:kk]]
[[Category:pages from Wikipedia11]]
[[Category:Pages and categories from Wikipedia]]
[[Category:kk]][[en:Help:Conditional expressions]][[Category:kk]]
[[en:Conditional expressions]][[Category:Pages and categories from Wikipedia]]
[[Category:kk]][[en:Help:Conditional expressions]][[Category:kk]]
[[en:Conditional expressions]]</text>
      <sha1>e89dhqhsckqe7e1ll1zk4ysp0p3yy6p</sha1>
    </revision>
    <revision>
      <id>677680</id>
      <parentid>677679</parentid>
      <timestamp>2025-01-17T12:59:12Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Adding [[Category:1 revision imported]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677680</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28352" sha1="3pc87yidsomawzr9ar338k4fx5jsm9p" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and categories from Wikipedia]]
[[Category:kk]][[en:Help:Conditional expressions]][[Category:kk]]
[[Category:pages from Wikipedia11]]
[[Category:Pages and categories from Wikipedia]]
[[Category:kk]][[en:Help:Conditional expressions]][[Category:kk]]
[[en:Conditional expressions]][[Category:Pages and categories from Wikipedia]]
[[Category:kk]][[en:Help:Conditional expressions]][[Category:kk]]
[[Category:1 revision imported]]
[[en:Conditional expressions]]</text>
      <sha1>3pc87yidsomawzr9ar338k4fx5jsm9p</sha1>
    </revision>
    <revision>
      <id>677681</id>
      <parentid>677680</parentid>
      <timestamp>2025-01-18T12:07:29Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Adding [[Category:bump-01-18]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677681</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28376" sha1="fobkzwyhrqjlnuib86rrwh5a8hpsi7j" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and categories from Wikipedia]]
[[Category:kk]][[en:Help:Conditional expressions]][[Category:kk]]
[[Category:pages from Wikipedia11]]
[[Category:Pages and categories from Wikipedia]]
[[Category:kk]][[en:Help:Conditional expressions]][[Category:kk]]
[[en:Conditional expressions]][[Category:Pages and categories from Wikipedia]]
[[Category:kk]][[en:Help:Conditional expressions]][[Category:kk]]
[[Category:1 revision imported]]
[[Category:bump-01-18]]
[[en:Conditional expressions]]</text>
      <sha1>fobkzwyhrqjlnuib86rrwh5a8hpsi7j</sha1>
    </revision>
    <revision>
      <id>677682</id>
      <parentid>677681</parentid>
      <timestamp>2025-01-22T15:17:45Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Removing from [[Category:1 revision imported]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677682</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28343" sha1="j426t0ztiile7fg6r0pj1tryigf069e" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and categories from Wikipedia]]
[[Category:kk]][[en:Help:Conditional expressions]][[Category:kk]]
[[Category:pages from Wikipedia11]]
[[Category:Pages and categories from Wikipedia]]
[[Category:kk]][[en:Help:Conditional expressions]][[Category:kk]]
[[en:Conditional expressions]][[Category:Pages and categories from Wikipedia]]
[[Category:kk]][[en:Help:Conditional expressions]][[Category:kk]]
[[Category:bump-01-18]]
[[en:Conditional expressions]]</text>
      <sha1>j426t0ztiile7fg6r0pj1tryigf069e</sha1>
    </revision>
    <revision>
      <id>677683</id>
      <parentid>677682</parentid>
      <timestamp>2025-01-23T20:06:36Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Removing from [[Category:Kk]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677683</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28250" sha1="su0w0u4vkucc1c8eo2e26mmszsmoocw" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[Category:pages from Wikipedia11]]
[[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[en:Conditional expressions]]</text>
      <sha1>su0w0u4vkucc1c8eo2e26mmszsmoocw</sha1>
    </revision>
    <revision>
      <id>677684</id>
      <parentid>677683</parentid>
      <timestamp>2025-01-26T20:29:06Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Moving from [[Category:Pages from Wikipedia11]] to [[Category:Pages from Wikipedia]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677684</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28248" sha1="a009d4hj0pyx9qx1tgzhztdyf9cv70t" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[Category:Pages from Wikipedia]]
[[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[en:Conditional expressions]]</text>
      <sha1>a009d4hj0pyx9qx1tgzhztdyf9cv70t</sha1>
    </revision>
    <revision>
      <id>677685</id>
      <parentid>677684</parentid>
      <timestamp>2025-01-26T20:54:34Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677685</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28310" sha1="3b86t2d0p9xf37jyoedotbxzx2t0k14" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]][[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[en:Conditional expressions]]</text>
      <sha1>3b86t2d0p9xf37jyoedotbxzx2t0k14</sha1>
    </revision>
    <revision>
      <id>677686</id>
      <parentid>677685</parentid>
      <timestamp>2025-01-26T22:30:07Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Adding [[Category:bump 1-26]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677686</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28333" sha1="excu347u4iwzfi7nbpa5f9js4eldink" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]][[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[en:Conditional expressions]]</text>
      <sha1>excu347u4iwzfi7nbpa5f9js4eldink</sha1>
    </revision>
    <revision>
      <id>677687</id>
      <parentid>677686</parentid>
      <timestamp>2025-01-28T15:02:48Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Adding [[Category:second attempt at refreshing on January 27 2024]][[Category:{{subst:FULLPAGENAME}}]][[Category:second attempt at refreshing on January 27 2024]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677687</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28495" sha1="f0gow2iox6bmgvcmbap9rka8m340yp8" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]][[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and categories from Wikipedia]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]</text>
      <sha1>f0gow2iox6bmgvcmbap9rka8m340yp8</sha1>
    </revision>
    <revision>
      <id>677688</id>
      <parentid>677687</parentid>
      <timestamp>2025-02-06T14:24:35Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677688</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28501" sha1="b33zozde89wvnynv1s709xvpexidt6f" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]][[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]</text>
      <sha1>b33zozde89wvnynv1s709xvpexidt6f</sha1>
    </revision>
    <revision>
      <id>677689</id>
      <parentid>677688</parentid>
      <timestamp>2025-02-06T17:28:32Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677689</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28501" sha1="8449j77i1qp1vgdsbaeskr0iv0004ol" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[category:MediaWiki Help|Conditional Expressions]]
[[category:Wikipedia template help|Conditional Expressions]]
[[category:Pages and Categories from Wikipedia 5]]
[[en:Help:Conditional expressions]][[category:Pages and Categories from Wikipedia 3]][[en:Conditional expressions]][[category:kk]]
[[category:Pages and Categories from Wikipedia 5]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[category:Pages and Categories from Wikipedia 5]]
[[en:Help:Conditional expressions]][[category:bump-01-18]]
[[category:bump 1-26]]
[[category:second attempt at refreshing on January 27 2024]][[category:Help:Conditional expressions]][[category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]</text>
      <sha1>8449j77i1qp1vgdsbaeskr0iv0004ol</sha1>
    </revision>
    <revision>
      <id>677690</id>
      <parentid>677689</parentid>
      <timestamp>2025-02-18T22:16:34Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677690</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28501" sha1="b33zozde89wvnynv1s709xvpexidt6f" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]][[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]</text>
      <sha1>b33zozde89wvnynv1s709xvpexidt6f</sha1>
    </revision>
    <revision>
      <id>677691</id>
      <parentid>677690</parentid>
      <timestamp>2025-02-19T22:18:06Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from Wikipedia 5]] to [[Category:qq-{{subst:NAMESPACE}}-qq]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677691</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28573" sha1="e9q9flzzlcfqlwkv72ybldpbb7vwizw" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]][[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]</text>
      <sha1>e9q9flzzlcfqlwkv72ybldpbb7vwizw</sha1>
    </revision>
    <revision>
      <id>677692</id>
      <parentid>677691</parentid>
      <timestamp>2025-02-19T23:12:59Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from Wikipedia 3]] to [[Category:qq-{{subst:NAMESPACE}}-qq]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677692</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28598" sha1="gduylli70suq05m7oxczv87gb3pp51x" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]</text>
      <sha1>gduylli70suq05m7oxczv87gb3pp51x</sha1>
    </revision>
    <revision>
      <id>677693</id>
      <parentid>677692</parentid>
      <timestamp>2025-02-20T02:45:37Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677693</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28615" sha1="gy1xv6rc0gc5rsqi7ooh9ozqoa3spmj" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]</text>
      <sha1>gy1xv6rc0gc5rsqi7ooh9ozqoa3spmj</sha1>
    </revision>
    <revision>
      <id>677694</id>
      <parentid>677693</parentid>
      <timestamp>2025-02-20T02:46:24Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677694</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28632" sha1="02je9ltgwzhgrdmohtrr62e0kojk9l3" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>02je9ltgwzhgrdmohtrr62e0kojk9l3</sha1>
    </revision>
    <revision>
      <id>677695</id>
      <parentid>677694</parentid>
      <timestamp>2025-02-20T02:59:09Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677695</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28649" sha1="r0tj9g5r6ixqdiete48gbfwbrmoctm7" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>r0tj9g5r6ixqdiete48gbfwbrmoctm7</sha1>
    </revision>
    <revision>
      <id>677696</id>
      <parentid>677695</parentid>
      <timestamp>2025-02-20T03:25:22Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677696</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28666" sha1="groz2fteuoc499onmaljpe91xpao4uu" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>groz2fteuoc499onmaljpe91xpao4uu</sha1>
    </revision>
    <revision>
      <id>677697</id>
      <parentid>677696</parentid>
      <timestamp>2025-02-20T03:38:43Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677697</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28738" sha1="3nmdsei0zeqc5ka9522tnpgkydf0o3f" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]][[Category:qq  3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>3nmdsei0zeqc5ka9522tnpgkydf0o3f</sha1>
    </revision>
    <revision>
      <id>677698</id>
      <parentid>677697</parentid>
      <timestamp>2025-02-20T03:40:18Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677698</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28810" sha1="ns5ewccxgc99lsria9wo83rdaqaqys7" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>ns5ewccxgc99lsria9wo83rdaqaqys7</sha1>
    </revision>
    <revision>
      <id>677699</id>
      <parentid>677698</parentid>
      <timestamp>2025-02-28T06:23:46Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677699</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28827" sha1="e1xxsnhmyyc31wxapnrrowk7214cn30" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>e1xxsnhmyyc31wxapnrrowk7214cn30</sha1>
    </revision>
    <revision>
      <id>677700</id>
      <parentid>677699</parentid>
      <timestamp>2025-03-08T18:21:23Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677700</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28876" sha1="pch2ovwkry580pv1ndlj0uj0crd7wmr" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia]]</text>
      <sha1>pch2ovwkry580pv1ndlj0uj0crd7wmr</sha1>
    </revision>
    <revision>
      <id>677701</id>
      <parentid>677700</parentid>
      <timestamp>2025-03-08T22:22:03Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Adding [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677701</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28937" sha1="qwqs6ehn6yhaizhc5z79agmyuf6goid" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]</text>
      <sha1>qwqs6ehn6yhaizhc5z79agmyuf6goid</sha1>
    </revision>
    <revision>
      <id>677702</id>
      <parentid>677701</parentid>
      <timestamp>2025-03-11T22:46:07Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677702</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29029" sha1="4mu8tt09weapm6xfn91xl4a1sqsdfh8" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:bump 1-26]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:New]]</text>
      <sha1>4mu8tt09weapm6xfn91xl4a1sqsdfh8</sha1>
    </revision>
    <revision>
      <id>677703</id>
      <parentid>677702</parentid>
      <timestamp>2025-03-12T09:19:45Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Removing from [[Category:Bump 1-26]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677703</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29006" sha1="ozbbs9zfbn73dww2xh5vi1dad2a6uxm" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:kk]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:New]]</text>
      <sha1>ozbbs9zfbn73dww2xh5vi1dad2a6uxm</sha1>
    </revision>
    <revision>
      <id>677704</id>
      <parentid>677703</parentid>
      <timestamp>2025-03-12T21:44:07Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Kk]] to [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677704</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29067" sha1="50gwkgb6j4f7uranshm7386tdb95je1" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:Kk]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:New]]</text>
      <sha1>50gwkgb6j4f7uranshm7386tdb95je1</sha1>
    </revision>
    <revision>
      <id>677705</id>
      <parentid>677704</parentid>
      <timestamp>2025-03-14T21:50:33Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677705</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29071" sha1="9t9vrzrq0rdm7fcd4l6u7ryi8bw01k7" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:qqHelp]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:New]]</text>
      <sha1>9t9vrzrq0rdm7fcd4l6u7ryi8bw01k7</sha1>
    </revision>
    <revision>
      <id>677706</id>
      <parentid>677705</parentid>
      <timestamp>2025-03-14T22:50:08Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Adding [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677706</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29132" sha1="h92p9jjhkzez4tjgh3lruoi8e1i2zxc" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:qqHelp]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:New]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]</text>
      <sha1>h92p9jjhkzez4tjgh3lruoi8e1i2zxc</sha1>
    </revision>
    <revision>
      <id>677707</id>
      <parentid>677706</parentid>
      <timestamp>2025-03-15T23:49:12Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677707</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29149" sha1="izzgsv8cy1dzosjt1z1zbm2hkb3x7v1" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  3]]
[[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:qqHelp]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  3]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:New]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:New]]</text>
      <sha1>izzgsv8cy1dzosjt1z1zbm2hkb3x7v1</sha1>
    </revision>
    <revision>
      <id>677708</id>
      <parentid>677707</parentid>
      <timestamp>2025-03-16T07:42:18Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Removing from [[Category:Qq 3]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677708</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29058" sha1="28siyhflm1h7ww6x15l4762dk9t3vbs" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:qqHelp]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:bump-01-18]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:New]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:New]]</text>
      <sha1>28siyhflm1h7ww6x15l4762dk9t3vbs</sha1>
    </revision>
    <revision>
      <id>677709</id>
      <parentid>677708</parentid>
      <timestamp>2025-03-17T19:58:14Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677709</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29050" sha1="1htn3gz071wqv1n1rgz83p42q5h7vhx" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq-Help-qq]]
[[en:Conditional expressions]][[Category:qqHelp]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:qq  5]]
[[Category:qq-Help-qq]]
[[en:Help:Conditional expressions]][[Category:qq]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:New]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:New]]</text>
      <sha1>1htn3gz071wqv1n1rgz83p42q5h7vhx</sha1>
    </revision>
    <revision>
      <id>677710</id>
      <parentid>677709</parentid>
      <timestamp>2025-03-19T20:47:50Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "qq" to "11"</comment>
      <origin>677710</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29050" sha1="0o0db5rzd7vzclj8xs3qp4jou61y7cf" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:11]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]</text>
      <sha1>0o0db5rzd7vzclj8xs3qp4jou61y7cf</sha1>
    </revision>
    <revision>
      <id>677711</id>
      <parentid>677710</parentid>
      <timestamp>2025-03-22T06:43:33Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677711</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29067" sha1="gwtkyymi11x1ajlw52yii0vy86z5aj1" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:11]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]][[Category:]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>gwtkyymi11x1ajlw52yii0vy86z5aj1</sha1>
    </revision>
    <revision>
      <id>677712</id>
      <parentid>677711</parentid>
      <timestamp>2025-03-22T22:37:58Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "[[Category:]]" to ""</comment>
      <origin>677712</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29054" sha1="gszollxj1iu89j1llo7obiu2k6579tq" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:11]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>gszollxj1iu89j1llo7obiu2k6579tq</sha1>
    </revision>
    <revision>
      <id>677713</id>
      <parentid>677712</parentid>
      <timestamp>2025-03-24T20:42:40Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677713</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29071" sha1="t59yqycuriek2s6bb0fojulshh9rher" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:11]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>t59yqycuriek2s6bb0fojulshh9rher</sha1>
    </revision>
    <revision>
      <id>677714</id>
      <parentid>677713</parentid>
      <timestamp>2025-03-25T04:43:56Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677714</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29088" sha1="8oxhc8wbpc1bn8nwehw7kqqnd2gcjbg" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:11]]
[[Category:second attempt at refreshing on January 27 2024]][[Category:Help:Conditional expressions]][[Category:second attempt at refreshing on January 27 2024]]
[[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>8oxhc8wbpc1bn8nwehw7kqqnd2gcjbg</sha1>
    </revision>
    <revision>
      <id>677715</id>
      <parentid>677714</parentid>
      <timestamp>2025-03-26T09:32:05Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Removing from [[Category:Second attempt at refreshing on January 27 2024]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677715</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28967" sha1="qszt9bwzgpcindwvnon73h15kug70xh" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11  5]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:11]]
[[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>qszt9bwzgpcindwvnon73h15kug70xh</sha1>
    </revision>
    <revision>
      <id>677716</id>
      <parentid>677715</parentid>
      <timestamp>2025-03-27T08:16:07Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Removing from [[Category:11 5]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677716</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28910" sha1="fgw3hf5ap4ijsuqbgj35cpn70pwm7ld" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:11]]
[[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>fgw3hf5ap4ijsuqbgj35cpn70pwm7ld</sha1>
    </revision>
    <revision>
      <id>677717</id>
      <parentid>677716</parentid>
      <timestamp>2025-03-27T12:34:48Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677717</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28927" sha1="rsflvk4uj05qqew0zcgr5r460d5ts95" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]][[Category: 5]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:11]]
[[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>rsflvk4uj05qqew0zcgr5r460d5ts95</sha1>
    </revision>
    <revision>
      <id>677718</id>
      <parentid>677717</parentid>
      <timestamp>2025-03-30T01:00:33Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from Wikipedia 5]] to [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677718</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29236" sha1="0m72kyi5hprxzbxau8120nl1xiva2yi" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category: 5]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category: 5]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category: 5]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category: 5]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:11]]
[[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>0m72kyi5hprxzbxau8120nl1xiva2yi</sha1>
    </revision>
    <revision>
      <id>677719</id>
      <parentid>677718</parentid>
      <timestamp>2025-03-30T01:25:10Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677719</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29253" sha1="lh3tgpmpw917dn32k36kee7lfejr8vv" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category: 5]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category: 5]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category: 5]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category: 5]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:11]]
[[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>lh3tgpmpw917dn32k36kee7lfejr8vv</sha1>
    </revision>
    <revision>
      <id>677720</id>
      <parentid>677719</parentid>
      <timestamp>2025-04-07T02:21:05Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Removing from [[Category:5]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677720</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29193" sha1="ctjpf0r9iwi9pttzz0nalxtel9s6v1t" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:11]]
[[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>ctjpf0r9iwi9pttzz0nalxtel9s6v1t</sha1>
    </revision>
    <revision>
      <id>677721</id>
      <parentid>677720</parentid>
      <timestamp>2025-04-08T20:43:15Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Removing from [[Category:Qq]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677721</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29038" sha1="js0pvljp9ywb4go2v24prykuxox3mh9" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:11]]
[[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:11]][[sn:Conditional expressions]][[Category:11]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>js0pvljp9ywb4go2v24prykuxox3mh9</sha1>
    </revision>
    <revision>
      <id>677722</id>
      <parentid>677721</parentid>
      <timestamp>2025-04-09T02:16:36Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Removing from [[Category:11]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677722</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28929" sha1="2r1wgtdpkelryk0m3kq41kyobxj6wui" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[sn:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[Category:New]]
[[sn:Conditional expressions]][[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]
[[Category:New]]</text>
      <sha1>2r1wgtdpkelryk0m3kq41kyobxj6wui</sha1>
    </revision>
    <revision>
      <id>677723</id>
      <parentid>677722</parentid>
      <timestamp>2025-04-09T05:30:41Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Removing from [[Category:New]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677723</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28726" sha1="ap4g4qo0guzptdqoabllg4ozmd4zg3g" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[sn:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>ap4g4qo0guzptdqoabllg4ozmd4zg3g</sha1>
    </revision>
    <revision>
      <id>677724</id>
      <parentid>677723</parentid>
      <timestamp>2025-04-15T21:58:39Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Copying from [[Category:Pages and Categories from Wikipedia 5]] to [[Category:qq]][[sn:{{subst:PAGENAME}}]][[Category:qq]] on using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677724</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29031" sha1="ktlf34qz1g6yic54c47scrgznnfatee" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[sn:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>ktlf34qz1g6yic54c47scrgznnfatee</sha1>
    </revision>
    <revision>
      <id>677725</id>
      <parentid>677724</parentid>
      <timestamp>2025-04-15T23:12:23Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <origin>677725</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29048" sha1="rmopkfmjpsq84v3g0f9xoq8slzhiqwz" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[sn:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]
[[Category:New]]</text>
      <sha1>rmopkfmjpsq84v3g0f9xoq8slzhiqwz</sha1>
    </revision>
    <revision>
      <id>677726</id>
      <parentid>677725</parentid>
      <timestamp>2025-04-27T09:39:10Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Removing from [[Category:New]] using [[c:Help:Cat-a-lot|Cat-a-lot]]</comment>
      <origin>677726</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="29031" sha1="ktlf34qz1g6yic54c47scrgznnfatee" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[sn:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[Category:qq]][[sn:Conditional expressions]][[Category:qq]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>ktlf34qz1g6yic54c47scrgznnfatee</sha1>
    </revision>
    <revision>
      <id>677727</id>
      <parentid>677726</parentid>
      <timestamp>2025-04-29T10:47:11Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "[[Category:qq]]" to ""</comment>
      <origin>677727</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28881" sha1="msc7rkj0w38dkg46ug92nniy4akk6sk" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Conditional expressions]][[Category:11Help]]
[[sn:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[Category:11-Help-11]]
[[en:Help:Conditional expressions]][[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>msc7rkj0w38dkg46ug92nniy4akk6sk</sha1>
    </revision>
    <revision>
      <id>677728</id>
      <parentid>677727</parentid>
      <timestamp>2025-05-18T01:19:59Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: remove deleted category 11-Help-11</comment>
      <origin>677728</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28789" sha1="c2n4yt8u5j79m3b57ttfb6daiymijh7" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Conditional expressions]][[Category:11Help]]
[[sn:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>c2n4yt8u5j79m3b57ttfb6daiymijh7</sha1>
    </revision>
    <revision>
      <id>677729</id>
      <parentid>677728</parentid>
      <timestamp>2025-05-18T01:22:03Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: remove deleted category 11Help</comment>
      <origin>677729</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28770" sha1="0nif6zpt38i0t25v4u7fgmch026gh5q" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Conditional expressions]]
[[sn:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[Category:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>0nif6zpt38i0t25v4u7fgmch026gh5q</sha1>
    </revision>
    <revision>
      <id>677730</id>
      <parentid>677729</parentid>
      <timestamp>2025-05-19T06:04:35Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: remove deleted category Help:Conditional expressions</comment>
      <origin>677730</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28729" sha1="7ix7mfvf8y8o28tagvfgrld7a1lqiyg" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Conditional expressions]]
[[sn:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>7ix7mfvf8y8o28tagvfgrld7a1lqiyg</sha1>
    </revision>
    <revision>
      <id>677731</id>
      <parentid>677730</parentid>
      <timestamp>2025-05-19T18:08:21Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>677731</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28748" sha1="pzdafo9z8waeihmg0is5cnuvwqtqufa" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[:en:Case-sensitive|Case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Conditional expressions]]
[[sn:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]][[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:Pages and Categories from Wikipedia 5]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>pzdafo9z8waeihmg0is5cnuvwqtqufa</sha1>
    </revision>
    <revision>
      <id>677732</id>
      <parentid>677731</parentid>
      <timestamp>2025-05-19T19:45:55Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: remove deleted category Pages and Categories from Wikipedia 5</comment>
      <origin>677732</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28498" sha1="bje622x2vqz4mh8cohypgkz6h59n8pd" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[:en:Case-sensitive|Case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>bje622x2vqz4mh8cohypgkz6h59n8pd</sha1>
    </revision>
    <revision>
      <id>677733</id>
      <parentid>677732</parentid>
      <timestamp>2025-05-19T22:29:54Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>677733</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28537" sha1="p5dwki312wmboplm2ynww8duzjshibp" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[:en:Case-sensitive|Case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[:en:Conditional (computer programming)|Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>p5dwki312wmboplm2ynww8duzjshibp</sha1>
    </revision>
    <revision>
      <id>677734</id>
      <parentid>677733</parentid>
      <timestamp>2025-05-24T19:18:23Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>677734</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28550" sha1="d5khtu5ds5jgapmxtnsl7qsj872nqxs" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[:en:Case-sensitive|Case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[:en:Redlinks|Redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[:en:Conditional (computer programming)|Conditional (computer programming)]]
* [[Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>d5khtu5ds5jgapmxtnsl7qsj872nqxs</sha1>
    </revision>
    <revision>
      <id>677735</id>
      <parentid>677734</parentid>
      <timestamp>2025-05-24T19:45:28Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>677735</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28574" sha1="rc86bthnisxmexivzwsjscr6w7pistb" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[:en:Case-sensitive|Case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[:en:Redlinks|Redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[:en:Conditional (computer programming)|Conditional (computer programming)]]
* [[:en:Relational operator|Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>rc86bthnisxmexivzwsjscr6w7pistb</sha1>
    </revision>
    <revision>
      <id>677736</id>
      <parentid>677735</parentid>
      <timestamp>2025-05-24T21:31:11Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>677736</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28578" sha1="0ro7x0o5zaoz5c4kvel582c1w5sm8j8" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[:en:Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[:en:Case-sensitive|Case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[:en:Redlinks|Redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[:en:Conditional (computer programming)|Conditional (computer programming)]]
* [[:en:Relational operator|Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>0ro7x0o5zaoz5c4kvel582c1w5sm8j8</sha1>
    </revision>
    <revision>
      <id>677737</id>
      <parentid>677736</parentid>
      <timestamp>2025-05-24T23:04:10Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>677737</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28600" sha1="fcmnq5xwzxuiouqm37ds63t68c36s2e" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[:en:Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[:en:Case-sensitive|Case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[:en:Redlinks|Redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[:en:Variadic template|Variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[:en:Conditional (computer programming)|Conditional (computer programming)]]
* [[:en:Relational operator|Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>fcmnq5xwzxuiouqm37ds63t68c36s2e</sha1>
    </revision>
    <revision>
      <id>677738</id>
      <parentid>677737</parentid>
      <timestamp>2025-05-25T00:10:00Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>Bot: convert local link → enwiki interwiki</comment>
      <origin>677738</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28617" sha1="pysd53cwj3g4f0t2ydtiztfbd0hnnkr" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[:en:Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[:en:Vertical bar|Vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[:en:Case-sensitive|Case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[:en:Redlinks|Redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[:en:Variadic template|Variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[:en:Conditional (computer programming)|Conditional (computer programming)]]
* [[:en:Relational operator|Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>pysd53cwj3g4f0t2ydtiztfbd0hnnkr</sha1>
    </revision>
    <revision>
      <id>2545</id>
      <timestamp>2025-05-25T05:50:36Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <comment>imported only latest revision from https://archive.org/details/miraheze-wiki-shintowiki-20251218 to avoid excessive disk usage, comment on the revision was Bot: convert local link → enwiki interwiki</comment>
      <origin>2545</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28638" sha1="e7id1367z791p8m1742oq057yb4nchb" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[:en:Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[:en:Vertical bar|Vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[:en:Case-sensitive|Case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[:en:Switch statement|Switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[:en:Redlinks|Redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[:en:Variadic template|Variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[:en:Conditional (computer programming)|Conditional (computer programming)]]
* [[:en:Relational operator|Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]
[[sn:Conditional expressions]]
[[sn:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]

[[sn:Conditional expressions]]
[[sn:Conditional expressions]][[sn:Conditional expressions]][[sn:Conditional expressions]]</text>
      <sha1>e7id1367z791p8m1742oq057yb4nchb</sha1>
    </revision>
    <revision>
      <id>359567</id>
      <parentid>2545</parentid>
      <timestamp>2026-02-20T01:49:14Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>Text replacement - "[[sn:" to "[[Category:sn_crud|TEST"</comment>
      <origin>359567</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28872" sha1="ajulx64r3f11ydnuswfrg51r44u3qzs" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[:en:Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[:en:Vertical bar|Vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[:en:Case-sensitive|Case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[:en:Switch statement|Switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[:en:Redlinks|Redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[:en:Variadic template|Variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[:en:Conditional (computer programming)|Conditional (computer programming)]]
* [[:en:Relational operator|Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]

[[Category:sn_crud|TESTConditional expressions]]
[[Category:sn_crud|TESTConditional expressions]]
[[en:Help:Conditional expressions]]
[[Category:sn_crud|TESTConditional expressions]]
[[Category:sn_crud|TESTConditional expressions]]
[[en:Conditional expressions]]
[[Category:sn_crud|TESTConditional expressions]]
[[Category:sn_crud|TESTConditional expressions]]
[[Category:sn_crud|TESTConditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]
[[Category:sn_crud|TESTConditional expressions]]
[[Category:sn_crud|TESTConditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]

[[Category:sn_crud|TESTConditional expressions]]
[[Category:sn_crud|TESTConditional expressions]][[Category:sn_crud|TESTConditional expressions]][[Category:sn_crud|TESTConditional expressions]]</text>
      <sha1>ajulx64r3f11ydnuswfrg51r44u3qzs</sha1>
    </revision>
    <revision>
      <id>3604361</id>
      <parentid>359567</parentid>
      <timestamp>2026-04-10T02:22:43Z</timestamp>
      <contributor>
        <username>EmmaBot</username>
        <id>703</id>
      </contributor>
      <comment>Bot: remove [[Category:Sn crud]] (crud category cleanup) [[github:EmmaLeonhart/shintowiki-scripts/actions/runs/24221752933|time triggered pipeline]]</comment>
      <origin>3604361</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="28236" sha1="55v21duu1snkke0592td58s27qt0fkm" xml:space="preserve">{{for|more detailed instructions|mw:Help:Extension:ParserFunctions}}
{{Wikipedia how-to|H:CONEX|H:CX}}
This page describes how to use various [[WP:Parser functions|parser functions]] to display different results based on detected conditions in a page or [[Help:Template|template]].

Here is a quick summary of these functions in terms of how they treat their input (the function names are linked to more detailed descriptions of them found below on this page):
* &lt;code&gt;[[#if]]&lt;/code&gt; checks the truth value of a string
* &lt;code&gt;[[#ifeq]]&lt;/code&gt; checks whether two strings or numbers are equal
* &lt;code&gt;[[#switch]]&lt;/code&gt; compares a string to a set of possible values
* &lt;code&gt;[[#expr]]&lt;/code&gt; evaluates a mathematical expression
* &lt;code&gt;[[#ifexpr]]&lt;/code&gt; evaluates a mathematical expression and acts on the truth value of the result
* &lt;code&gt;[[#iferror]]&lt;/code&gt; checks whether a string (often an expression inside of &lt;code&gt;#expr&lt;/code&gt;) triggers a parser error
* &lt;code&gt;[[#ifexist]]&lt;/code&gt; checks whether a page with a given title exists on the wiki (including image/media files)

For the use of these functions in tables, see [[Help:Conditional tables]].

== Summary ==
The basic syntax (and use) of each function is as follows:
* &lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string is true or false)
* &lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if unequal'' }}&lt;/code&gt;
*: (selects one of two values based on whether the two strings are equal—a numerical comparison is done whenever that is possible)
* &lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default'' }}&lt;/code&gt;
*: (chooses between multiple alternatives based on the value of the test string—basically equivalent to a chain of &lt;code&gt;#ifeq&lt;/code&gt; tests, but ''much'' more efficient)
* &lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;
*: (evaluates a given expression; see [[Help:Calculation]] for details)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;
*: (selects one of two values based on the truth value of an evaluated expression)
* &lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if no error'' }}&lt;/code&gt;
*: (selects one of two values based on whether the test string generates a parser error)
* &lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if exists'' | ''value if doesn't exist'' }}&lt;/code&gt;
*: (selects one of two values depending on the existence of a page having the given title)

{{anchor|Truth}}Note that "truth" is interpreted very differently by the string-based &lt;code&gt;[[#if]]&lt;/code&gt; function and the numerically oriented functions &lt;code&gt;[[#expr]]&lt;/code&gt; and &lt;code&gt;[[#ifexpr]]&lt;/code&gt;. A string is considered true if it contains at least one non-whitespace character (thus, for example, the &lt;code&gt;#if&lt;/code&gt; function interprets the strings "0" and "FALSE" as true values, not false). Any string containing only whitespace or no characters at all will be treated as false (thus &lt;code&gt;#if&lt;/code&gt; interprets "&amp;nbsp;" and "", as well as undefined parameters, as false values). On the other hand, in the expressions evaluated by &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt;, Boolean operators like &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt; interpret the numerical value 0 as false and any other number as true. In terms of output, Boolean operations return 1 for a true value and 0 for false (and these are treated as ordinary numbers by the numerical operators). Non-numerical strings (including most uses of empty strings and undefined parameters) will cause &lt;code&gt;#expr&lt;/code&gt; and &lt;code&gt;#ifexpr&lt;/code&gt; to report an error.

Also note that all leading and trailing [[:en:Whitespace character|whitespace]] within each of the parts of a parser function call gets stripped out, allowing the calls to be formatted with extra whitespace for better readability. For example:
&lt;!-- Note: change the next example in this section if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | parameter xx value is true
   | parameter xx value is false
}}
&lt;/syntaxhighlight&gt;
Here, only the spaces in the strings "parameter xx value is true" and "parameter xx value is false" are significant. All other whitespace is ignored. Thus, the construct above is equivalent to the following call:
&lt;!-- Note: change the preceding example if you change this one! --&gt;
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if:{{{xx|}}}|parameter xx value is true|parameter xx value is false}}
&lt;/syntaxhighlight&gt;

In any part of a parser function call where a string is expected, one can use a literal string, a template call, a parser function call, or some other [[Help:Magic words|magic word]].

== &lt;span id="if"&gt;&lt;/span&gt;Using &lt;code&gt;#if&lt;/code&gt; ==
: ''See also: the {{tlx|if}} template''
The &lt;code&gt;#if&lt;/code&gt; function selects one of two alternatives based on the truth value of a test string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#if:&lt;/nowiki&gt; ''test string'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

[[#Truth|As explained above]], a string is considered true if it contains at least one non-whitespace character. Any string containing only whitespace or no characters at all will be treated as false.

Undefined parameter values are tricky: if the first positional parameter was not defined in the template call, then &lt;code&gt;&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the literal string "&lt;nowiki&gt;{{{1}}}&lt;/nowiki&gt;" (i.e., the 7-character string containing three sets of curly braces around the number 1), which is a ''true'' value. (This problem exists for both [[Help:Template#Handling parameters|named and positional]] parameters.) But &lt;code&gt;&lt;nowiki&gt;{{{1|}}}&lt;/nowiki&gt;&lt;/code&gt; will evaluate to the empty string (a ''false'' value) because the [[:en:Vertical bar|Vertical bar]] or pipe character, &lt;code&gt;"|"&lt;/code&gt;, immediately following the parameter name specifies a default value (here an empty string because there is nothing between the pipe and the first closing curly brace) as a "fallback" value to be used if the parameter is undefined.

This function can be used to check whether a parameter has been passed to a template with a true value.

Examples:
&lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{1|}}}
   | first positional parameter has a true value
   | first positional parameter has a false value (or no value)
}}

{{#if: {{{xx|}}}
   | named parameter xx has a true value
   | named parameter xx has a false value (or no value)
}}

{{#if: {{{xx|}}}{{{yy|}}}
   | either xx or yy has a true value, or both are true
   | both xx and yy have a false value (or no value)
}}
&lt;/syntaxhighlight&gt;

Testing whether both of two parameters are true requires a little more effort. For example:

{|
| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | {{#if: {{{yy|}}}
       | both xx and yy have true values
       | xx is true but not yy
     }}
   | both xx and yy are false
}}
&lt;/syntaxhighlight&gt;

| &lt;syntaxhighlight lang="wikitext"&gt;
{{#if: {{{xx|}}}
   | xx is true (and yy is not tested)
   | {{#if: {{{yy|}}}
       | yy is true but not xx
       | both xx and yy are false
     }}
}}
&lt;/syntaxhighlight&gt;
|}

Note that nesting &lt;code&gt;#if&lt;/code&gt; functions like this gets very resource-intensive very fast. On some wikis, even seven levels of nesting might exceed resource limits.

See the discussion of &lt;code&gt;[[#ifeq]]&lt;/code&gt; for how to detect whether a parameter has been defined regardless of whether its value is true or false.

== &lt;span id="ifeq"&gt;&lt;/span&gt;Using &lt;code&gt;#ifeq&lt;/code&gt; ==
: ''See also: the {{tlx|ifeq}} template''
The &lt;code&gt;#ifeq&lt;/code&gt; function selects one of two alternatives based on whether two test strings are equal to each other.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifeq:&lt;/nowiki&gt; ''string 1'' | ''string 2'' | ''value if equal'' | ''value if not equal'' }}&lt;/code&gt;'''

If both strings are valid numerical values, they are compared as numbers, rather than as literal strings:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 01 | 1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 01 | 1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: x01 | x1 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: x01 | x1 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.000 | 002 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.000 | 002 | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2.5 | 2+.5 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2.5 | 2+.5 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2*10^3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2*10^3 | 2000 | equal | not equal }}''' (arithmetic!)
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: 2E3 | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: 2E3 | 2000 | equal | not equal }}'''

As seen in the 4th and 5th examples, mathematical expressions are ''not'' evaluated. They are treated as regular strings. But &lt;code&gt;[[#expr]]&lt;/code&gt; can be used to evaluate such expressions.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr:2*10^3}} | 2000 | equal | not equal }}'''

String comparisons are [[:en:Case-sensitive|Case-sensitive]]:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: King | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: King | king | equal | not equal }}'''

For case-insensitive checking, use [[Help:Magic words#Formatting|the &lt;code&gt;&lt;nowiki&gt;{{lc:}}&lt;/nowiki&gt;&lt;/code&gt; or &lt;code&gt;&lt;nowiki&gt;{{uc:}}&lt;/nowiki&gt;&lt;/code&gt; function]] to force the strings to all lower- or upper-case. This is most useful when dealing with parameter values:

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc:King}} | king | equal | not equal }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{lc:King}} | king | equal | not equal }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{lc: {{{position}}} }} | top | code if true | code if false }}&lt;/nowiki&gt;&lt;/code&gt;

In the second example, the values "top", "Top", and "TOP" will all result in successful matches.

This parser function can be used to detect whether a template parameter is defined, even if it has been set to a false value. For example, to check whether the first positional parameter has been passed to a template (note that the strings "&lt;code&gt;+&lt;/code&gt;" and "&lt;code&gt;-&lt;/code&gt;" can be any two different non-whitespace strings):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined | 1 is undefined }}&lt;/nowiki&gt;&lt;/code&gt;

To be specific, here's what this code generates when called in the following ways:

: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;}}&lt;/code&gt; → '''1 is undefined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=}}&lt;/code&gt; → '''1 is defined'''
: &lt;code&gt;&lt;nowiki&gt;{{&lt;/nowiki&gt;&lt;var&gt;template-name&lt;/var&gt;|1=foo}}&lt;/code&gt; → '''1 is defined'''

See [[mw:Help:Extension:ParserFunctions##ifeq]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="switch"&gt;&lt;/span&gt;Using &lt;code&gt;#switch&lt;/code&gt; ==

The &lt;code&gt;#switch&lt;/code&gt; function selects between multiple alternatives based on an input string.

: '''&lt;code&gt;&lt;nowiki&gt;{{#switch:&lt;/nowiki&gt; ''test string'' | ''case1'' = ''value for case 1'' | ... | ''default value'' }}&lt;/code&gt;'''

Equivalent to the [[:en:Switch statement|Switch statement]] found in some programming languages, it is a convenient way of dealing with multiple cases without having to chain lots of &lt;code&gt;#if&lt;/code&gt; functions together. However, note that performance suffers when there are more than 100 alternatives. Placing common values earlier in the list of cases can cause the function to execute significantly faster.

For each case, either side of the equals sign "&lt;code&gt;=&lt;/code&gt;" can be a simple string, a call to a parser function (including &lt;code&gt;#expr&lt;/code&gt; to evaulate expressions), or a template call. If any cases are not associated with a value (i.e., no equals sign is used), the next specified value will be used. This allows multiple cases to share the same value without having to specify that value repeatedly (as seen in the example below).

If no matching case is found, then the default value is used. This is usually specified last with no associated "''case''" value, as seen in the syntax summary above, but it can also be specified at any point after the test string if the construct &lt;code&gt;| #default = ''value''&lt;/code&gt; is used (see the second example below). If no default is specified in either way, then a null string will be returned when no cases match the input string.

This function in particular benefits from being set up in a multiline format.

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5
 | 6
 | 7 = in the range 5 to 7
 | other
}}
&lt;/syntaxhighlight&gt;

Here, if the value of the template parameter &lt;code&gt;x&lt;/code&gt; is the string "1", then the output will be the string "one"; if "2", then "two"; and so forth. But for any of the values "5", "6" or "7", the output will be the string "in the range 5 to 7". For any other value, or a null value, it will return the string "other".

The following example is equivalent to the previous one:

&lt;syntaxhighlight lang="wikitext"&gt;
{{#switch: {{{x|}}}
 | #default = other
 | 1 = one
 | 2 = two
 | 3 = three
 | 4 = four
 | 5|6|7 = in the range 5 to 7
}}
&lt;/syntaxhighlight&gt;

See [[Help:Switch parser function]] for a full description and more examples.

== &lt;span id="expr"&gt;&lt;/span&gt;Using &lt;code&gt;#expr&lt;/code&gt; ==
The &lt;code&gt;#expr&lt;/code&gt; function evaluates mathematical (including Boolean) expressions. While not itself a conditional function, it is often used inside of those functions, so it is briefly described here. See [https://www.mediawiki.org/wiki/Manual:Expr_parser_function_syntax Manual:Expr parser function syntax] for further details.

: '''&lt;code&gt;&lt;nowiki&gt;{{#expr:&lt;/nowiki&gt; ''expression'' }}&lt;/code&gt;'''

Unlike the &lt;code&gt;[[#if]]&lt;/code&gt; function, all values in the &lt;code&gt;''expression''&lt;/code&gt; evaluated by &lt;code&gt;#expr&lt;/code&gt; are assumed to be numerical. It does not work with arbitrary strings. For the purpose of its Boolean operations (e.g., &lt;code&gt;and&lt;/code&gt;, &lt;code&gt;or&lt;/code&gt;, and &lt;code&gt;not&lt;/code&gt;), false is represented by 0 and true by 1; these values are treated as regular numbers by the numerical operators. As input, the Boolean operators treat 0 as false and every other number as true. If any non-numerical strings are used, an error will result.

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ( {{{1}}}+{{{xshift}}} - 6 ) * 18.4 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: ln(7)^3 - abs(-0.344) + floor(5/3) round 3 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#expr: {{{n}}}&gt;0 and {{{n}}}&lt;1.0 }}&lt;/syntaxhighlight&gt;

Note that these examples assume that all parameters referred to are defined and have numerical values. If this is not the case, errors will occur. See &lt;code&gt;[[#iferror]]&lt;/code&gt; for one way to handle errors.

== &lt;span id="ifexpr"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexpr&lt;/code&gt; ==
The &lt;code&gt;#ifexpr&lt;/code&gt; function evaluates an expression in exactly the same way &lt;code&gt;[[#expr]]&lt;/code&gt; does (see the explanation of that function for details), but returns one of two possible values depending on the truth value of the result.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexpr:&lt;/nowiki&gt; ''expression'' | ''value if true'' | ''value if false'' }}&lt;/code&gt;'''

Examples:
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: ( {{{1}}} + {{{2}}} ) * 2.63 &gt; 45 | above 45 | not above 45 }}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifexpr: {{{1}}} &gt; 0 and {{{1}}} &lt; 1.0 or {{#ifeq:{{{decimal}}}|yes}} | is decimal | not decimal }}&lt;/syntaxhighlight&gt;

As [[#Truth|explained above]], for the purposes of this function, 0 is false and any other numerical value is true.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 0 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 2 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 2 | yes | no }}'''

An empty input expression evaluates to false.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: | yes | no }}'''

But invalid input, including input containing non-numerical strings, will display an error message.

: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: = | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: = | yes | no }}'''
: &lt;code&gt;&lt;nowiki&gt;{{#ifexpr: A=B | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: A=B | yes | no }}'''

Under normal circumstances, the function &lt;code&gt;#ifexpr&lt;/code&gt; is equivalent to using the following construct with &lt;code&gt;[[#ifeq]]&lt;/code&gt; and &lt;code&gt;[[#expr]]&lt;/code&gt; (note the reversal of the "true" and "false" output values, since we are comparing the input result to "0"):

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr:&lt;/nowiki&gt; ''expression'' }} | 0 | ''value if false'' | ''value if true'' }}&lt;/code&gt;

But any error message generated by &lt;code&gt;#expr&lt;/code&gt; will be seen by &lt;code&gt;#ifeq&lt;/code&gt; as an ordinary string that is not equal to "0", and thus the &lt;code&gt;''value if true''&lt;/code&gt; will be output.

: &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{#expr: = }} | 0 | yes | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifeq: {{#expr: = }} | 0 | yes | no }}'''

Either or both of the output values may be omitted (or left empty), in which case (assuming the "missing" branch of the construct is reached) no output results.

:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | yes }}&lt;/nowiki&gt;&lt;/code&gt; → '''{{#ifexpr: 1 &gt; 0 | yes }}'''
:&lt;code&gt;&lt;nowiki&gt;{{#ifexpr: 1 &gt; 0 | | no }}&lt;/nowiki&gt;&lt;/code&gt; → '''&lt;nowiki /&gt;{{#ifexpr: 1 &gt; 0 | | no }}'''
&lt;!-- note: &lt;nowiki /&gt; is used in the last example to prevent wiki markup (i.e., apostrophes) from appearing on the page --&gt;
{{tip|To make comparisons based on dates (e.g., if the current date and time is after some other date and time), first convert the time(s) to the number of seconds after January 1, 1970, using the function [[Help:Time function|&lt;nowiki&gt;{{#time: U }}&lt;/nowiki&gt;]], then compare (or add, subtract, etc.) those numerical values.}}

See [[mw:Help:Extension:ParserFunctions##ifexpr]]&lt;!-- the two "#" characters are necessary --&gt; for more details, including possible counterintuitive results due to the way this function is implemented.

== &lt;span id="iferror"&gt;&lt;/span&gt;Using &lt;code&gt;#iferror&lt;/code&gt; ==
The &lt;code&gt;#iferror&lt;/code&gt; function selects one of two alternatives depending on whether its input triggers an error.

: '''&lt;code&gt;&lt;nowiki&gt;{{#iferror:&lt;/nowiki&gt; ''test string'' | ''value if error'' | ''value if correct'' }}&lt;/code&gt;'''

The test string is typically a call to &lt;code&gt;[[#expr]]&lt;/code&gt; or some other parser function, but can be plain text or a template call, in which case an error would be triggered by any HTML object with &lt;code&gt;class="error"&lt;/code&gt; or any of various [[mw:Help:Templates|template]] errors such as loops and recursions,&lt;!-- different or the same? --&gt; or some other "failsoft"&lt;!-- what is that? --&gt; parser error.

One or both of the return values can be omitted. If the &lt;code&gt;''value if correct''&lt;/code&gt; is omitted, the test string is returned if it is not erroneous. If the &lt;code&gt;''value if error''&lt;/code&gt; is also omitted, an empty string is returned on an error:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} | error }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + X }} | error }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + 2 }} }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: 1 + 2 }} }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: 1 + X }} }}&lt;/syntaxhighlight&gt; → '''&lt;nowiki /&gt;{{#iferror: {{#expr: 1 + X }} }}'''&lt;!-- nowiki prevents an apostrophe from appearing on the page --&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: {{#expr: . }} | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: {{#expr: . }} | error | correct }}'''
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}&lt;/syntaxhighlight&gt; → '''{{#iferror: &lt;strong class="error"&gt;a&lt;/strong&gt; | error | correct }}'''

== &lt;span id="ifexist"&gt;&lt;/span&gt;Using &lt;code&gt;#ifexist&lt;/code&gt; ==
{{shortcut|HELP:ifexist}}
The &lt;code&gt;#ifexist&lt;/code&gt; function selects one of two alternatives depending on whether a page exists at the specified title.

: '''&lt;code&gt;&lt;nowiki&gt;{{#ifexist:&lt;/nowiki&gt; ''page title'' | ''value if page exists'' | ''value if page doesn't exist'' }}&lt;/code&gt;'''

The page can be in any [[Help:Namespace|namespace]], so it can be an article or "content page", an image or other media file, a Category, etc. The actual content of the page is irrelevant, so it may be empty or a [[Help:Redirect|redirect]]. Titles that would result in [[:en:Redlinks|Redlinks]] do not exist (and, as with redlinks, checking for the existence of a nonexistent page causes the title to appear on [[Special:Wantedpages]]).

The checking is extremely fast, but has been limited to 500 instances per page because it is considered an "[[mw:Manual:$wgExpensiveParserFunctionLimit|expensive parser function]]". (However, multiple checks of the ''same'' title on the same page do not count as multiple instances, because the results of the first check is cached and reused for the subsequent checks.)

== &lt;span id="Conditionals for templates"&gt;&lt;/span&gt;Checking for template parameters ==
A common idiom encountered in template coding is the "chain of fallback values", as seen in this example:

: &lt;code&gt;&lt;nowiki&gt;{{{&lt;/nowiki&gt;1|{{color|purple|&lt;nowiki&gt;{{{url|&lt;/nowiki&gt;{{color|darkgreen|&lt;nowiki&gt;{{{URL|}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;}}&lt;nowiki&gt;}}}&lt;/nowiki&gt;&lt;/code&gt;&lt;!-- note: can't use whitespace to pretty this up! --&gt;

Here, if the first positional parameter is defined, then its value will be used. If it is undefined, then the parameter named &lt;code&gt;url&lt;/code&gt; will be checked and if it is defined, then its value will be used. If both the first positional parameter and &lt;code&gt;url&lt;/code&gt; are undefined, then the parameter named &lt;code&gt;URL&lt;/code&gt; is checked: if it is defined, its value is used; if not, then the empty string will be used.

The problem is, this construct tends to be interpreted as being equivalent to:

: &lt;code&gt;&lt;nowiki&gt;{{#if: {{{1|}}} | {{{1}}} |&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{url|}}} | {{{url}}} |&lt;/nowiki&gt; {{color|darkgreen|&lt;nowiki&gt;{{#if: {{{URL|}}} | {{{URL}}} }}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;}} &lt;nowiki&gt;}}&lt;/nowiki&gt;&lt;/code&gt;

and it is '''not'''. The difference is that the first construct, using default values, depends on the ''definedness'' of the parameters, whereas the second construct, using &lt;code&gt;#if&lt;/code&gt; functions, depends on the ''truth value'' of the parameters. These are two very different things.

If any of the parameters in the chain of fallback values (in the first construct) have been set to an empty value (or only whitespace) in the template call, then that empty value will be used instead of "falling back" to the next parameter in the chain. So, for example, if the template is called in either of these two ways:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name||url=https://example.com/}}&lt;/syntaxhighlight&gt;
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=https://example.com/|}}&lt;/syntaxhighlight&gt;

The first positional parameter ''has'' been defined: its value is the empty string. Thus, the value of &lt;code&gt;url&lt;/code&gt; is irrelevant. The template never "gets to" that value.

Similarly, if the template is called in this way:

: &lt;syntaxhighlight lang="wikitext" inline&gt;{{template-name|url=|URL=https://example.com/}}&lt;/syntaxhighlight&gt;

The empty value of &lt;code&gt;url&lt;/code&gt; will be used instead of the value of &lt;code&gt;URL&lt;/code&gt;.

These examples might seem a bit contrived, since they rely on the template being called in "the wrong way". But it is surprisingly easy to run across cases where these problems occur with perfectly reasonable template calls (especially if there is a "hierarchy" of templates, where one template calls another, passing on the values of its parameters to the parameters of the "lower-level" template—in such cases, one will often end up with defined-but-empty parameter values).

Because of issues like this, template coders sometimes find themselves needing to distinguish between different combinations of three states: defined-and-non-blank, defined-and-blank, and undefined. This can be done in the following ways (the first positional parameter is used here, but named parameters work the same way).

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; undefined or defined-and-blank
: &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|}}} | 1 is defined and contains non-whitespace | 1 is undefined, empty, or contains only whitespace }}&lt;/syntaxhighlight&gt;

; Defined (whether non-blank or blank) &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#ifeq: {{{1|/}}}{{{1|}}} | / | 1 is undefined | 1 is defined (and possibly empty or only-whitespace) }}&lt;/syntaxhighlight&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{#if: {{{1|/}}} | {{{1|}}} | / }} | 1 is defined (and possibly empty or only-whitespace) | 1 is undefined }}&lt;/syntaxhighlight&gt;

Note that the &lt;code&gt;+&lt;/code&gt; and &lt;code&gt;-&lt;/code&gt; characters can be any two different non-whitespace characters. Also, if you just want to use the value of the parameter when it's defined and some other value when it's undefined, you can use the simpler "fallback" construct:

: &lt;code&gt;&lt;nowiki&gt;{{{1|&lt;/nowiki&gt;''some other value''}}}&lt;/code&gt;

; Defined-and-non-blank &amp;nbsp; vs. &amp;nbsp; defined-and-blank &amp;nbsp; vs. &amp;nbsp; undefined
: The following are equivalent alternatives:
:* &lt;code&gt;&lt;nowiki&gt;{{#ifeq: {{{1|+}}} | {{{1|-}}} | 1 is defined&lt;/nowiki&gt; {{color|purple|&lt;nowiki&gt;{{#if: {{{1|}}} | and non-blank | and blank }}&lt;/nowiki&gt;}} | 1 undefined }}&lt;/code&gt;
:* &lt;syntaxhighlight lang="wikitext" inline&gt;{{#if: {{{1|/}}} | {{#if:{{{1|}}} | 1 is defined and non-blank | 1 is undefined }} | 1 is defined and blank }}&lt;/syntaxhighlight&gt;

If you don't care about the undefined case, you can remove the "&lt;code&gt;|1 undefined&lt;/code&gt;" in both examples.

== Variadic templates ==
Wikitext syntax does not allow natively to create truly [[:en:Variadic template|Variadic template]]s, but only pseudo-variadic ones, that check the incoming parameters one by one until a certain fixed amount. It is possible to break this limitation however by using dedicated modules. For simple cases, {{ml|separated entries|main}} allows to expand all sequential parameters blindly and has the ability to set custom delimiters. For more complex cases, {{ml|params|}} allows to count, list, map, filter and propagate all incoming parameters without knowing their number in advance.

== See also ==
* [[Help:Conditional tables]]
* [[mw:Help:Extension:ParserFunctions]], a longer and more technical help page at MediaWiki.org
* [[:en:Conditional (computer programming)|Conditional (computer programming)]]
* [[:en:Relational operator|Relational operator]]
* [[Help:Template]] and [[Wikipedia:Template sandbox and test cases]]
* [[m:Help:Advanced templates]]
* [[:Category:If-then-else templates]]
{{Wikipedia technical help|collapsed}}
[[Category:MediaWiki Help|Conditional Expressions]]
[[Category:Wikipedia template help|Conditional Expressions]]

[[en:Help:Conditional expressions]]
[[en:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]
[[en:Help:Conditional expressions]][[en:Conditional expressions]]</text>
      <sha1>55v21duu1snkke0592td58s27qt0fkm</sha1>
    </revision>
  </page>
</mediawiki>
