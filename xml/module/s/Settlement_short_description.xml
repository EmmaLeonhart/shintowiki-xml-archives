<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>Shinto Wiki</sitename>
    <dbname>shintowiki</dbname>
    <base>https://shinto.miraheze.org/wiki/Main_Page</base>
    <generator>MediaWiki 1.45.3</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="420" case="first-letter">GeoJson</namespace>
      <namespace key="421" case="first-letter">GeoJson talk</namespace>
      <namespace key="828" case="first-letter">Module</namespace>
      <namespace key="829" case="first-letter">Module talk</namespace>
      <namespace key="860" case="first-letter">Item</namespace>
      <namespace key="861" case="first-letter">Item talk</namespace>
      <namespace key="862" case="first-letter">Property</namespace>
      <namespace key="863" case="first-letter">Property talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Module:Settlement short description</title>
    <ns>828</ns>
    <id>107029</id>
    <revision>
      <id>3083804</id>
      <timestamp>2019-01-19T14:40:06Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>save</comment>
      <origin>3083804</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1558" sha1="ou6u8pzezuoqy1pslrz192zf9vpvc5a" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.main(frame)
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	local subdivision_name = plain(args[3])
	local subdivision_name1 = plain(args[4])
	local subdivision_name2 = plain(args[5])
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	if not settlement_type then settlement_type = 'Place' end
	if not subdivision_name then return p.shortdesc(settlement_type, frame, 1, true) end
	if (string.find(settlement_type, subdivision_name)
		or (subdivision_name1 and string.find(settlement_type, subdivision_name1, 1, true))) then
		return categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat}
	else
		if not(subdivision_name2 and string.find(settlement_type, '[nN]eighbo[u]?rhood')) then
			subdivision_name2 = nil
		end
		return p.shortdesc(string.format('%s in %s', settlement_type,
			table.concat(tableTools.compressSparseArray({subdivision_name2, subdivision_name1, subdivision_name}), ', ')), frame)
	end
end
return p</text>
      <sha1>ou6u8pzezuoqy1pslrz192zf9vpvc5a</sha1>
    </revision>
    <revision>
      <id>3083805</id>
      <parentid>3083804</parentid>
      <timestamp>2019-01-19T14:51:08Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Galobtter moved page [[Module:Galobtter/sandbox/Infobox settlement short description]] to [[Module:Sandbox/Galobtter/Infobox settlement short description]] without leaving a redirect: oops!</comment>
      <origin>3083804</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1558" sha1="ou6u8pzezuoqy1pslrz192zf9vpvc5a" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.main(frame)
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	local subdivision_name = plain(args[3])
	local subdivision_name1 = plain(args[4])
	local subdivision_name2 = plain(args[5])
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	if not settlement_type then settlement_type = 'Place' end
	if not subdivision_name then return p.shortdesc(settlement_type, frame, 1, true) end
	if (string.find(settlement_type, subdivision_name)
		or (subdivision_name1 and string.find(settlement_type, subdivision_name1, 1, true))) then
		return categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat}
	else
		if not(subdivision_name2 and string.find(settlement_type, '[nN]eighbo[u]?rhood')) then
			subdivision_name2 = nil
		end
		return p.shortdesc(string.format('%s in %s', settlement_type,
			table.concat(tableTools.compressSparseArray({subdivision_name2, subdivision_name1, subdivision_name}), ', ')), frame)
	end
end
return p</text>
      <sha1>ou6u8pzezuoqy1pslrz192zf9vpvc5a</sha1>
    </revision>
    <revision>
      <id>3083806</id>
      <parentid>3083805</parentid>
      <timestamp>2019-01-19T15:04:54Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083806</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1608" sha1="7bcu0sa8299vdwhnrdvsldioxtzmwc1" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.main(frame)
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	local subdivision_name = plain(args[3])
	local subdivision_name1 = plain(args[4])
	local subdivision_name2 = plain(args[5])
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	if not settlement_type then settlement_type = 'Place' end
	if not subdivision_name then return p.shortdesc(settlement_type, frame, 1, true) end
	if (string.find(settlement_type, subdivision_name)
		or (subdivision_name1 and string.find(settlement_type, subdivision_name1, 1, true))) then
		return categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat}
	else
		if not(subdivision_name2 and
			(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
			subdivision_name2 = nil
		end
		return p.shortdesc(string.format('%s in %s', settlement_type,
			table.concat(tableTools.compressSparseArray({subdivision_name2, subdivision_name1, subdivision_name}), ', ')), frame)
	end
end
return p</text>
      <sha1>7bcu0sa8299vdwhnrdvsldioxtzmwc1</sha1>
    </revision>
    <revision>
      <id>3083807</id>
      <parentid>3083806</parentid>
      <timestamp>2019-01-20T08:53:08Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Galobtter moved page [[Module:Sandbox/Galobtter/Infobox settlement short description]] to [[Module:Settlement short description]] without leaving a redirect: +</comment>
      <origin>3083806</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="1608" sha1="7bcu0sa8299vdwhnrdvsldioxtzmwc1" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.main(frame)
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	local subdivision_name = plain(args[3])
	local subdivision_name1 = plain(args[4])
	local subdivision_name2 = plain(args[5])
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	if not settlement_type then settlement_type = 'Place' end
	if not subdivision_name then return p.shortdesc(settlement_type, frame, 1, true) end
	if (string.find(settlement_type, subdivision_name)
		or (subdivision_name1 and string.find(settlement_type, subdivision_name1, 1, true))) then
		return categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat}
	else
		if not(subdivision_name2 and
			(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
			subdivision_name2 = nil
		end
		return p.shortdesc(string.format('%s in %s', settlement_type,
			table.concat(tableTools.compressSparseArray({subdivision_name2, subdivision_name1, subdivision_name}), ', ')), frame)
	end
end
return p</text>
      <sha1>7bcu0sa8299vdwhnrdvsldioxtzmwc1</sha1>
    </revision>
    <revision>
      <id>3083808</id>
      <parentid>3083807</parentid>
      <timestamp>2019-01-20T09:31:52Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fixes</comment>
      <origin>3083808</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2362" sha1="2p8a0rnidypbsjm3wxbxyejxecpwmch" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local redundant = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(redundant) do --series of replacements
		location:gsub(i, v)
	end
end

function p.main(frame)
	local categories = ""
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	local subdivision_name = plain(args[3])
	local subdivision_name1 = plain(args[4])
	local subdivision_name2 = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	first_redundant = string.find(settlement_type, subdivision_name, 1, true)
	second_redundant = subdivision_name1 and string.find(settlement_type, subdivision_name1, 1, true)
	
	if first_redundant or second_redundant then
		categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat}
	end
	
	if first_redundant then subdivision_name = nil end --don't display redundant location
	if second_redundant then subdivision_name1 = nil end --don't display redundant location
	
	if not(subdivision_name2 and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_name2 = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray({subdivision_name2, subdivision_name1, subdivision_name}), ', ')
	
	p.cleanupLoc (location)
	
	return p.shortdesc(string.format('%s in %s', settlement_type, location), frame)..categories
end

return p</text>
      <sha1>2p8a0rnidypbsjm3wxbxyejxecpwmch</sha1>
    </revision>
    <revision>
      <id>3083809</id>
      <parentid>3083808</parentid>
      <timestamp>2019-01-20T09:33:14Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>hm</comment>
      <origin>3083809</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2390" sha1="mj8ezdlrfbpqms7uiynbgn4ti01djvv" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local redundant = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(redundant) do --series of replacements
		location:gsub(i, v)
	end
	return location
end

function p.main(frame)
	local categories = ""
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	local subdivision_name = plain(args[3])
	local subdivision_name1 = plain(args[4])
	local subdivision_name2 = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	first_redundant = string.find(settlement_type, subdivision_name, 1, true)
	second_redundant = subdivision_name1 and string.find(settlement_type, subdivision_name1, 1, true)
	
	if first_redundant or second_redundant then
		categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat}
	end
	
	if first_redundant then subdivision_name = nil end --don't display redundant location
	if second_redundant then subdivision_name1 = nil end --don't display redundant location
	
	if not(subdivision_name2 and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_name2 = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray({subdivision_name2, subdivision_name1, subdivision_name}), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(string.format('%s in %s', settlement_type, location), frame)..categories
end

return p</text>
      <sha1>mj8ezdlrfbpqms7uiynbgn4ti01djvv</sha1>
    </revision>
    <revision>
      <id>3083810</id>
      <parentid>3083809</parentid>
      <timestamp>2019-01-20T09:38:02Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>hm</comment>
      <origin>3083810</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2401" sha1="fb25gitp7964txd6lydryekfe69p1bu" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local redundant = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(redundant) do --series of replacements
		location:gsub(i, v)
		return i
	end
	return location
end

function p.main(frame)
	local categories = ""
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	local subdivision_name = plain(args[3])
	local subdivision_name1 = plain(args[4])
	local subdivision_name2 = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	first_redundant = string.find(settlement_type, subdivision_name, 1, true)
	second_redundant = subdivision_name1 and string.find(settlement_type, subdivision_name1, 1, true)
	
	if first_redundant or second_redundant then
		categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat}
	end
	
	if first_redundant then subdivision_name = nil end --don't display redundant location
	if second_redundant then subdivision_name1 = nil end --don't display redundant location
	
	if not(subdivision_name2 and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_name2 = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray({subdivision_name2, subdivision_name1, subdivision_name}), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(string.format('%s in %s', settlement_type, location), frame)..categories
end

return p</text>
      <sha1>fb25gitp7964txd6lydryekfe69p1bu</sha1>
    </revision>
    <revision>
      <id>3083811</id>
      <parentid>3083810</parentid>
      <timestamp>2019-01-20T09:38:46Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>sigh</comment>
      <origin>3083811</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2401" sha1="rywxbg1h2y06l1wuawpu4n1ic2jh5j7" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local redundant = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(redundant) do --series of replacements
		location = location:gsub(i, v)
	end
	return location
end

function p.main(frame)
	local categories = ""
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	local subdivision_name = plain(args[3])
	local subdivision_name1 = plain(args[4])
	local subdivision_name2 = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	first_redundant = string.find(settlement_type, subdivision_name, 1, true)
	second_redundant = subdivision_name1 and string.find(settlement_type, subdivision_name1, 1, true)
	
	if first_redundant or second_redundant then
		categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat}
	end
	
	if first_redundant then subdivision_name = nil end --don't display redundant location
	if second_redundant then subdivision_name1 = nil end --don't display redundant location
	
	if not(subdivision_name2 and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_name2 = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray({subdivision_name2, subdivision_name1, subdivision_name}), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(string.format('%s in %s', settlement_type, location), frame)..categories
end

return p</text>
      <sha1>rywxbg1h2y06l1wuawpu4n1ic2jh5j7</sha1>
    </revision>
    <revision>
      <id>3083812</id>
      <parentid>3083811</parentid>
      <timestamp>2019-01-20T09:53:09Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>hm</comment>
      <origin>3083812</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2166" sha1="01v66nr694y00rpifufaoatzswdcv60" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local redundant = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(redundant) do --series of replacements
		location = location:gsub(i, v)
	end
	return location
end

function p.main(frame)
	local categories = ""
	subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	subdivisions[1] = plain(args[3])
	subdivisions[2] = plain(args[4])
	subdivisions[3] = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivision[1] = nil --don't display redundancy
			categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat} --categorize
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray(subdivisions), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(string.format('%s in %s', settlement_type, location), frame)..categories
end

return p</text>
      <sha1>01v66nr694y00rpifufaoatzswdcv60</sha1>
    </revision>
    <revision>
      <id>3083813</id>
      <parentid>3083812</parentid>
      <timestamp>2019-01-20T09:53:41Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083813</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2172" sha1="stmbgj0ldmdvikhquf5nddit2ver25a" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local redundant = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(redundant) do --series of replacements
		location = location:gsub(i, v)
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	subdivisions[1] = plain(args[3])
	subdivisions[2] = plain(args[4])
	subdivisions[3] = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivision[1] = nil --don't display redundancy
			categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat} --categorize
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray(subdivisions), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(string.format('%s in %s', settlement_type, location), frame)..categories
end

return p</text>
      <sha1>stmbgj0ldmdvikhquf5nddit2ver25a</sha1>
    </revision>
    <revision>
      <id>3083814</id>
      <parentid>3083813</parentid>
      <timestamp>2019-01-20T09:54:06Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>hm</comment>
      <origin>3083814</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2173" sha1="b14uazrnr0moh6jzj0jw3m69sy81i5s" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local redundant = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(redundant) do --series of replacements
		location = location:gsub(i, v)
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	subdivisions[1] = plain(args[3])
	subdivisions[2] = plain(args[4])
	subdivisions[3] = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat} --categorize
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray(subdivisions), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(string.format('%s in %s', settlement_type, location), frame)..categories
end

return p</text>
      <sha1>b14uazrnr0moh6jzj0jw3m69sy81i5s</sha1>
    </revision>
    <revision>
      <id>3083815</id>
      <parentid>3083814</parentid>
      <timestamp>2019-01-20T10:07:04Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>hm</comment>
      <origin>3083815</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2269" sha1="2qjkseyufo2hm0enrzl632ztj7kbwgg" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
end

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	subdivisions[1] = plain(args[3])
	subdivisions[2] = plain(args[4])
	subdivisions[3] = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat} --categorize
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil
	end
	
	local location = table.concat(p.reverseTable(tableTools.compressSparseArray(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(string.format('%s in %s', settlement_type, location), frame)..categories
end

return p</text>
      <sha1>2qjkseyufo2hm0enrzl632ztj7kbwgg</sha1>
    </revision>
    <revision>
      <id>3083816</id>
      <parentid>3083815</parentid>
      <timestamp>2019-01-20T10:07:34Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>hm</comment>
      <origin>3083816</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2282" sha1="bgylxz0tca74kiamc6khml3jln7jcqb" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	subdivisions[1] = plain(args[3])
	subdivisions[2] = plain(args[4])
	subdivisions[3] = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat} --categorize
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil
	end
	
	local location = table.concat(p.reverseTable(tableTools.compressSparseArray(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(string.format('%s in %s', settlement_type, location), frame)..categories
end

return p</text>
      <sha1>bgylxz0tca74kiamc6khml3jln7jcqb</sha1>
    </revision>
    <revision>
      <id>3083817</id>
      <parentid>3083816</parentid>
      <timestamp>2019-01-20T10:08:37Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083817</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2282" sha1="bd19gk7z43vaezq0eu2ozk12omfju3g" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1])
	local short_description = plain(args[2])
	subdivisions[1] = plain(args[3])
	subdivisions[2] = plain(args[4])
	subdivisions[3] = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat} --categorize
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(string.format('%s in %s', settlement_type, location), frame)..categories
end

return p</text>
      <sha1>bd19gk7z43vaezq0eu2ozk12omfju3g</sha1>
    </revision>
    <revision>
      <id>3083818</id>
      <parentid>3083817</parentid>
      <timestamp>2019-01-20T10:15:15Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083818</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2288" sha1="qsr84f2fzmj75o1pukzs90i9tzxkrz6" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1]) or ""
	local short_description = plain(args[2])
	subdivisions[1] = plain(args[3])
	subdivisions[2] = plain(args[4])
	subdivisions[3] = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat} --categorize
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(string.format('%s in %s', settlement_type, location), frame)..categories
end

return p</text>
      <sha1>qsr84f2fzmj75o1pukzs90i9tzxkrz6</sha1>
    </revision>
    <revision>
      <id>3083819</id>
      <parentid>3083818</parentid>
      <timestamp>2019-01-20T10:18:42Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083819</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2293" sha1="ta5newpifz3b0nrzve95q79krneydso" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1]) or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = plain(args[3])
	subdivisions[2] = plain(args[4])
	subdivisions[3] = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat} --categorize
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(string.format('%s in %s', settlement_type, location), frame)..categories
end

return p</text>
      <sha1>ta5newpifz3b0nrzve95q79krneydso</sha1>
    </revision>
    <revision>
      <id>3083820</id>
      <parentid>3083819</parentid>
      <timestamp>2019-01-20T10:20:37Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>ix</comment>
      <origin>3083820</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2290" sha1="ny7jw17atx59q4usafyupx4hf8n7t4s" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1]) or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = plain(args[3])
	subdivisions[2] = plain(args[4])
	subdivisions[3] = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat} --categorize
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(settlement_type..(location and " in " + location), frame)..categories
end

return p</text>
      <sha1>ny7jw17atx59q4usafyupx4hf8n7t4s</sha1>
    </revision>
    <revision>
      <id>3083821</id>
      <parentid>3083820</parentid>
      <timestamp>2019-01-20T10:21:03Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083821</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2291" sha1="42k8uek2jn9neapt8xklg5dr2c1hrze" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1]) or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = plain(args[3])
	subdivisions[2] = plain(args[4])
	subdivisions[3] = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat} --categorize
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(settlement_type..(location and " in " .. location), frame)..categories
end

return p</text>
      <sha1>42k8uek2jn9neapt8xklg5dr2c1hrze</sha1>
    </revision>
    <revision>
      <id>3083822</id>
      <parentid>3083821</parentid>
      <timestamp>2019-01-20T10:22:07Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <minor/>
      <comment>Protected "[[Module:Settlement short description]]": [[WP:High-risk templates|High-risk Lua module]] ([Edit=Require template editor access] (indefinite) [Move=Require template editor access] (indefinite))</comment>
      <origin>3083821</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2291" sha1="42k8uek2jn9neapt8xklg5dr2c1hrze" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1]) or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = plain(args[3])
	subdivisions[2] = plain(args[4])
	subdivisions[3] = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat} --categorize
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(settlement_type..(location and " in " .. location), frame)..categories
end

return p</text>
      <sha1>42k8uek2jn9neapt8xklg5dr2c1hrze</sha1>
    </revision>
    <revision>
      <id>3083823</id>
      <parentid>3083822</parentid>
      <timestamp>2019-01-20T10:26:56Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083823</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2359" sha1="2wfnwsezllzsjm7z11546t3m7xpz61x" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description usin {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = plain(args[1]) or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = plain(args[3])
	subdivisions[2] = plain(args[4])
	subdivisions[3] = plain(args[5])
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			categories = categoryHandler{'[[Category:Infobox settlement pages with bad settlement type]]', nocat = args.nocat} --categorize
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(settlement_type..(location and " in " .. location), frame)..categories
end

return p</text>
      <sha1>2wfnwsezllzsjm7z11546t3m7xpz61x</sha1>
    </revision>
    <revision>
      <id>3083824</id>
      <parentid>3083823</parentid>
      <timestamp>2019-01-20T11:25:29Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083824</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="2987" sha1="nylzqjwzjoakdrgk8qq3cnqpsuj9l53" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["United States of America"] = "United States",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(settlement_type..(location and " in " .. location), frame)..categories
end

return p</text>
      <sha1>nylzqjwzjoakdrgk8qq3cnqpsuj9l53</sha1>
    </revision>
    <revision>
      <id>3083825</id>
      <parentid>3083824</parentid>
      <timestamp>2019-01-20T11:27:00Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083825</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3025" sha1="k1onj95moblmx3squcxjo2hciduq7w0" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["United States of America"] = "United States",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(settlement_type..(location and " in " .. location), frame)..categories
end

return p</text>
      <sha1>k1onj95moblmx3squcxjo2hciduq7w0</sha1>
    </revision>
    <revision>
      <id>3083826</id>
      <parentid>3083825</parentid>
      <timestamp>2019-01-20T11:28:55Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083826</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3064" sha1="rsfk4nyq7rajre4p1uirwmjeqg8fy49" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["United States of America"] = "United States",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(settlement_type..(location and " in " .. location), frame)..categories
end

return p</text>
      <sha1>rsfk4nyq7rajre4p1uirwmjeqg8fy49</sha1>
    </revision>
    <revision>
      <id>3083827</id>
      <parentid>3083826</parentid>
      <timestamp>2019-01-20T11:30:17Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083827</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3066" sha1="am7skvft74hcp4bnbdlfvdiw2ocs9r3" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["United States of America"] = "United States",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	return p.shortdesc(settlement_type..(location and (" in " .. location)), frame)..categories
end

return p</text>
      <sha1>am7skvft74hcp4bnbdlfvdiw2ocs9r3</sha1>
    </revision>
    <revision>
      <id>3083828</id>
      <parentid>3083827</parentid>
      <timestamp>2019-01-20T11:32:02Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083828</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3090" sha1="ef5vbr9ina62ntdbpadcs7elozrizwa" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["United States of America"] = "United States",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	if location then location =  " in " .. location end
	return p.shortdesc(settlement_type..location, frame)..categories
end

return p</text>
      <sha1>ef5vbr9ina62ntdbpadcs7elozrizwa</sha1>
    </revision>
    <revision>
      <id>3083829</id>
      <parentid>3083828</parentid>
      <timestamp>2019-01-20T11:32:44Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083829</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3109" sha1="dyhgxlxyy9jcdb978m68u7ov61wx45h" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["United States of America"] = "United States",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	if location then location =  " in " .. location else location = "" end
	return p.shortdesc(settlement_type..location, frame)..categories
end

return p</text>
      <sha1>dyhgxlxyy9jcdb978m68u7ov61wx45h</sha1>
    </revision>
    <revision>
      <id>3083830</id>
      <parentid>3083829</parentid>
      <timestamp>2019-01-20T11:35:21Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083830</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3112" sha1="0i7zb5kcze7d3e62nm7stmn07mt2ys6" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["United States of America"] = "United States",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..categories
end

return p</text>
      <sha1>0i7zb5kcze7d3e62nm7stmn07mt2ys6</sha1>
    </revision>
    <revision>
      <id>3083831</id>
      <parentid>3083830</parentid>
      <timestamp>2019-01-20T11:43:09Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083831</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3154" sha1="0r620ylsryhvn30b936n6ajzqldhuix" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["United States of America"] = "United States",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..categories
end

return p</text>
      <sha1>0r620ylsryhvn30b936n6ajzqldhuix</sha1>
    </revision>
    <revision>
      <id>3083832</id>
      <parentid>3083831</parentid>
      <timestamp>2019-01-20T11:47:25Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083832</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3158" sha1="j8zqqgk9rt5m6htzwxe285zycw7y05y" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["United States of America"] = "United States",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..categories
end

return p</text>
      <sha1>j8zqqgk9rt5m6htzwxe285zycw7y05y</sha1>
    </revision>
    <revision>
      <id>3083833</id>
      <parentid>3083832</parentid>
      <timestamp>2019-01-20T11:51:02Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>rm</comment>
      <origin>3083833</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3108" sha1="5wd5tx0jeu3uyawnsxnmhfeavi1ytf3" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..categories
end

return p</text>
      <sha1>5wd5tx0jeu3uyawnsxnmhfeavi1ytf3</sha1>
    </revision>
    <revision>
      <id>3083834</id>
      <parentid>3083833</parentid>
      <timestamp>2019-01-20T11:54:57Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>_</comment>
      <origin>3083834</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3142" sha1="geue8t2sw124fy5it192p4qekwa3x0z" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..categories
end

return p</text>
      <sha1>geue8t2sw124fy5it192p4qekwa3x0z</sha1>
    </revision>
    <revision>
      <id>3083835</id>
      <parentid>3083834</parentid>
      <timestamp>2019-01-20T11:55:57Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083835</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3147" sha1="a7mk5nsf7up4e9e6nh85spk8b7f5f9w" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local categories = ""
local args
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	args = getArgs (frame, {frameOnly = true})
	local categories = ""
	local subdivisions = {}
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..categories
end

return p</text>
      <sha1>a7mk5nsf7up4e9e6nh85spk8b7f5f9w</sha1>
    </revision>
    <revision>
      <id>3083836</id>
      <parentid>3083835</parentid>
      <timestamp>2019-01-20T11:56:09Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083836</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3147" sha1="jakju5m9qpg4vee4wr7f4voaaequcve" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local args
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	categories = categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	args = getArgs (frame, {frameOnly = true})
	local categories = ""
	local subdivisions = {}
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..categories
end

return p</text>
      <sha1>jakju5m9qpg4vee4wr7f4voaaequcve</sha1>
    </revision>
    <revision>
      <id>3083837</id>
      <parentid>3083836</parentid>
      <timestamp>2019-01-20T12:06:23Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083837</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3149" sha1="1444bqvqwafz7ts5oe2ukrpj9zdoxi8" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local args
p.categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cat)
	p.categories = p.categories..categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cat), nocat = args.nocat} --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	args = getArgs (frame, {frameOnly = true})
	local categories = ""
	local subdivisions = {}
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>1444bqvqwafz7ts5oe2ukrpj9zdoxi8</sha1>
    </revision>
    <revision>
      <id>3083838</id>
      <parentid>3083837</parentid>
      <timestamp>2019-01-20T12:07:30Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>Undid revision 879312977 by [[Special:Contributions/Galobtter|Galobtter]] ([[User talk:Galobtter|talk]])</comment>
      <origin>3083838</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3203" sha1="o9csudjuvmgq6nwg71d3m9aukruw820" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local args
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cattype)
	local category = categoryHandler{string.format('[[Category:Infobox settlement pages with bad %s]]', cattype), nocat = args.nocat}
	if category then categories = categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	args = getArgs (frame, {frameOnly = true})
	local categories = ""
	local subdivisions = {}
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..categories
end

return p</text>
      <sha1>o9csudjuvmgq6nwg71d3m9aukruw820</sha1>
    </revision>
    <revision>
      <id>3083839</id>
      <parentid>3083838</parentid>
      <timestamp>2019-01-20T12:08:48Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>rm</comment>
      <origin>3083839</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3166" sha1="tl54wxrhdivwpmwb832gwun0uy7s6nt" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local args
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local categoryHandler = require( 'Module:Category handler' ).main
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then categories = categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	args = getArgs (frame, {frameOnly = true})
	local categories = ""
	local subdivisions = {}
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..categories
end

return p</text>
      <sha1>tl54wxrhdivwpmwb832gwun0uy7s6nt</sha1>
    </revision>
    <revision>
      <id>3083840</id>
      <parentid>3083839</parentid>
      <timestamp>2019-01-20T12:09:03Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>rm</comment>
      <origin>3083840</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3100" sha1="j2o9t2lghjmjs8aicwmwhnkanf0520w" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
local args
local categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then categories = categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	args = getArgs (frame, {frameOnly = true})
	local categories = ""
	local subdivisions = {}
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..categories
end

return p</text>
      <sha1>j2o9t2lghjmjs8aicwmwhnkanf0520w</sha1>
    </revision>
    <revision>
      <id>3083841</id>
      <parentid>3083840</parentid>
      <timestamp>2019-01-20T12:10:45Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083841</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3097" sha1="plht6detoltgfkouwsifl9b09mxeqim" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>plht6detoltgfkouwsifl9b09mxeqim</sha1>
    </revision>
    <revision>
      <id>3083842</id>
      <parentid>3083841</parentid>
      <timestamp>2019-01-20T12:19:05Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083842</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3162" sha1="g3uaucubcw5v1su57ylnk9gbu2er8l9" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter
			:gsub('%b()', '') --remove things in brackets as generally not relevant
			:gsub("%s+", " ") --remove any possible redundant spaces from the above
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>g3uaucubcw5v1su57ylnk9gbu2er8l9</sha1>
    </revision>
    <revision>
      <id>3083843</id>
      <parentid>3083842</parentid>
      <timestamp>2019-01-20T12:25:19Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083843</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3448" sha1="ajox68gi1t01rqtcv768c55x8yl5sd8" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter..',' --comma at the end makes things convenient
	parameter = parameter:gsub('%b()', ', ') --remove things in brackets as etxtraneous information
			   :gsub('(,%s-),', '%1') --fix possible blank seperated commas from previous cleanup
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^[%s,]*', '') --trim commas and spaces from beginning
			   :gsub('[%s,]*$', '') --trim commas and spaces from end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>ajox68gi1t01rqtcv768c55x8yl5sd8</sha1>
    </revision>
    <revision>
      <id>3083844</id>
      <parentid>3083843</parentid>
      <timestamp>2019-01-20T12:27:36Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083844</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3258" sha1="6nrkrkg7hfm130wf5rdyb1mdfenk7em" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as etxtraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>6nrkrkg7hfm130wf5rdyb1mdfenk7em</sha1>
    </revision>
    <revision>
      <id>3083845</id>
      <parentid>3083844</parentid>
      <timestamp>2019-01-20T12:47:22Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083845</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3333" sha1="1agi7umsvz9qw6nm6qc8ce07qgnzk30" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as etxtraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London",
		["London, England"] = "London"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>1agi7umsvz9qw6nm6qc8ce07qgnzk30</sha1>
    </revision>
    <revision>
      <id>3083846</id>
      <parentid>3083845</parentid>
      <timestamp>2019-01-20T12:51:36Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083846</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3489" sha1="rlzai5xe163n8f30u0m0jn3saxvli14" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as etxtraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London",
		["London, England"] = "London"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local sep
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			if string.find(settlement_type, " in ") then sep = ", " end --if there is already an "in" in settlement_type, no need to add another
			p.category ("settlement type")
		end
	end

	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  (sep or " in ") .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>rlzai5xe163n8f30u0m0jn3saxvli14</sha1>
    </revision>
    <revision>
      <id>3083847</id>
      <parentid>3083846</parentid>
      <timestamp>2019-01-20T12:54:51Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>Undid revision 879317264 by [[Special:Contributions/Galobtter|Galobtter]] ([[User talk:Galobtter|talk]])</comment>
      <origin>3083847</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3333" sha1="8q1vah5btoq05de25es06c321jjjhaw" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as etxtraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London",
		["London, England"] = "London"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>8q1vah5btoq05de25es06c321jjjhaw</sha1>
    </revision>
    <revision>
      <id>3083848</id>
      <parentid>3083847</parentid>
      <timestamp>2019-01-20T12:59:02Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>+</comment>
      <origin>3083848</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3333" sha1="ec9nswn8zoqq36q5pvq6lnkafrfub3g" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as etxtraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London",
		["London, England"] = "London"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>ec9nswn8zoqq36q5pvq6lnkafrfub3g</sha1>
    </revision>
    <revision>
      <id>3083849</id>
      <parentid>3083848</parentid>
      <timestamp>2019-01-21T08:44:51Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>noreplace</comment>
      <origin>3083849</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3346" sha1="j7hu3olp6hn738yb98gmtllvd2m617h" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as etxtraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London",
		["London, England"] = "London"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	for x, y in ipairs (subdivisions) do
		if (y and string.find(settlement_type, y, 1, true)) then --if the subdivision is found within the settlement type
			subdivisions[x] = nil --don't display redundancy
			p.category ("settlement type")
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>j7hu3olp6hn738yb98gmtllvd2m617h</sha1>
    </revision>
    <revision>
      <id>3083850</id>
      <parentid>3083849</parentid>
      <timestamp>2019-01-21T13:00:20Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>rm redundancy to the title</comment>
      <origin>3083850</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3522" sha1="h712v0tcyzih4sh7ukssazv7g36v2se" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text/sandbox')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as etxtraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London",
		["London, England"] = "London"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	for x, y in ipairs (subdivisions) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivisions[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivisions
				subdivisions[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>h712v0tcyzih4sh7ukssazv7g36v2se</sha1>
    </revision>
    <revision>
      <id>3083851</id>
      <parentid>3083850</parentid>
      <timestamp>2019-01-21T18:40:26Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>oops</comment>
      <origin>3083851</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3514" sha1="1lk0jy70bc2rnkj5wxntoh06zpjyjr5" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Infobox settlement pages with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as etxtraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London",
		["London, England"] = "London"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	for x, y in ipairs (subdivisions) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivisions[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivisions
				subdivisions[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>1lk0jy70bc2rnkj5wxntoh06zpjyjr5</sha1>
    </revision>
    <revision>
      <id>3083852</id>
      <parentid>3083851</parentid>
      <timestamp>2019-08-15T11:06:55Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Fayenatic london</username>
        <id>0</id>
      </contributor>
      <comment>update category per [[WP:CFDS]], as demonstrated by [[user:Galobtter]] at [[Module:Settlement short description/sandbox]]</comment>
      <origin>3083852</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="3520" sha1="47bgtej7mf73qrzdgu9vmpvso8lihsr" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Pages using infobox settlement with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as etxtraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London",
		["London, England"] = "London"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivisions = {}
	local args = getArgs (frame, {frameOnly = true})
	local settlement_type = p.validate(plain(args[1]), "settlement type") or "Place"
	local short_description = plain(args[2])
	subdivisions[1] = p.validate(plain(args[3]))
	subdivisions[2] = p.validate(plain(args[4]))
	subdivisions[3] = p.validate(plain(args[5]))
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	if not(subdivisions[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivisions[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	for x, y in ipairs (subdivisions) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivisions[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivisions
				subdivisions[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivisions)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>47bgtej7mf73qrzdgu9vmpvso8lihsr</sha1>
    </revision>
    <revision>
      <id>3083853</id>
      <parentid>3083852</parentid>
      <timestamp>2019-08-20T04:18:54Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>update per Template_talk:Infobox_settlement#Sub-optimal_automatic_short_descriptions_for_Polish_locations</comment>
      <origin>3083853</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4302" sha1="brqb27qwjlx8t2vpw269k4mf602dkwf" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

function p.assign (args, argname, num)
	var = {}
	for i = 0,num do
		--handle initial "subdivision_foo" without number
		if i == 0 then
			val = ""
		else
			val = tostring(i)
		end
		var[i+1] = p.validate(plain(args[argname..val]))
	end
	return var
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Pages using infobox settlement with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as etxtraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London",
		["London, England"] = "London"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivision_types = {}
	local subdivision_names = {}
	local args = getArgs (frame, {parentOnly = true})
	local settlement_type = p.validate(plain(args.settlement_type or args.type), "settlement type") or "Place"
	local short_description = plain(args.short_description)
	subdivision_types = p.assign(args, "subdivision_type", 2)
	subdivision_names = p.assign(args, "subdivision_name", 2)
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	if not(subdivision_names[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_names[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	--If say "Voivodeship" is found within the subdivision_type, then specially handle
	--by adding Voivodeship to the end if not already so
	for x, y in ipairs (subdivision_types) do
		local special_types = {
			"Voivodeship"
		}
		for i, j in ipairs(special_types) do
			if string.find(y, j, 1, true) and not string.find(subdivision_names[x], j, 1, true) then
				subdivision_names[x] = subdivision_names[x].." "..j
			end
		end
	end
	
	for x, y in ipairs (subdivision_names) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivision_names[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivision_names
				subdivision_names[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivision_names)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>brqb27qwjlx8t2vpw269k4mf602dkwf</sha1>
    </revision>
    <revision>
      <id>3083854</id>
      <parentid>3083853</parentid>
      <timestamp>2019-08-23T06:08:26Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix</comment>
      <origin>3083854</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4348" sha1="c29dx94xer1rcc04lcg4xylmuug5m9g" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

function p.assign (args, argname, num)
	local val
	local var = {}
	for i = 0,num do
		--handle initial "subdivision_foo" without number
		if i == 0 then
			val = ""
		else
			val = tostring(i)
		end
		var[i+1] = p.validate(plain(args[argname..val]))
	end
	return var
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Pages using infobox settlement with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as etxtraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London",
		["London, England"] = "London"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivision_types = {}
	local subdivision_names = {}
	local args = getArgs (frame, {parentOnly = true})
	local settlement_type = p.validate(plain(args.settlement_type or args.type), "settlement type") or "Place"
	local short_description = plain(args.short_description)
	subdivision_types = p.assign(args, "subdivision_type", 2)
	subdivision_names = p.assign(args, "subdivision_name", 2)
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	if not(subdivision_names[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_names[3] = nil --only display the third subdivision_type if suburb or neighborhood
	end
	
	--If say "Voivodeship" is found within the subdivision_type, then specially handle
	--by adding Voivodeship to the end if not already so
	for x, y in ipairs (subdivision_types) do
		local special_types = {
			"Voivodeship"
		}
		for i, j in ipairs(special_types) do
			if subdivision_names[x] and string.find(y, j, 1, true)
				and not string.find(subdivision_names[x], j, 1, true) then
				subdivision_names[x] = subdivision_names[x].." "..j
			end
		end
	end
	
	for x, y in ipairs (subdivision_names) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivision_names[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivision_names
				subdivision_names[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivision_names)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>c29dx94xer1rcc04lcg4xylmuug5m9g</sha1>
    </revision>
    <revision>
      <id>3083855</id>
      <parentid>3083854</parentid>
      <timestamp>2020-02-25T14:06:03Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jonesey95</username>
        <id>0</id>
      </contributor>
      <comment>copy edit comments. No change to code</comment>
      <origin>3083855</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4354" sha1="fh465g8n27pabp1qraewqb9vufcgw90" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

function p.assign (args, argname, num)
	local val
	local var = {}
	for i = 0,num do
		--handle initial "subdivision_foo" without number
		if i == 0 then
			val = ""
		else
			val = tostring(i)
		end
		var[i+1] = p.validate(plain(args[argname..val]))
	end
	return var
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Pages using infobox settlement with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as extraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivisions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London",
		["London, England"] = "London"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivision_types = {}
	local subdivision_names = {}
	local args = getArgs (frame, {parentOnly = true})
	local settlement_type = p.validate(plain(args.settlement_type or args.type), "settlement type") or "Place"
	local short_description = plain(args.short_description)
	subdivision_types = p.assign(args, "subdivision_type", 2)
	subdivision_names = p.assign(args, "subdivision_name", 2)
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	if not(subdivision_names[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_names[3] = nil --display the third subdivision_type only if suburb or neighborhood
	end
	
	--if say "Voivodeship" is found within the subdivision_type, then specially handle
	--by adding Voivodeship to the end if not already present
	for x, y in ipairs (subdivision_types) do
		local special_types = {
			"Voivodeship"
		}
		for i, j in ipairs(special_types) do
			if subdivision_names[x] and string.find(y, j, 1, true)
				and not string.find(subdivision_names[x], j, 1, true) then
				subdivision_names[x] = subdivision_names[x].." "..j
			end
		end
	end
	
	for x, y in ipairs (subdivision_names) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivision_names[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivision_names
				subdivision_names[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivision_names)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>fh465g8n27pabp1qraewqb9vufcgw90</sha1>
    </revision>
    <revision>
      <id>3083856</id>
      <parentid>3083855</parentid>
      <timestamp>2021-04-19T20:42:52Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Galobtter</username>
        <id>0</id>
      </contributor>
      <comment>fix cleanups</comment>
      <origin>3083856</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4329" sha1="3c5crspyokscrdg2absr2vdfp0zlk4h" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

function p.assign (args, argname, num)
	local val
	local var = {}
	for i = 0,num do
		--handle initial "subdivision_foo" without number
		if i == 0 then
			val = ""
		else
			val = tostring(i)
		end
		var[i+1] = p.validate(plain(args[argname..val]))
	end
	return var
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Pages using infobox settlement with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as extraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivisions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London, England"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivision_types = {}
	local subdivision_names = {}
	local args = getArgs (frame, {parentOnly = true})
	local settlement_type = p.validate(plain(args.settlement_type or args.type), "settlement type") or "Place"
	local short_description = plain(args.short_description)
	subdivision_types = p.assign(args, "subdivision_type", 2)
	subdivision_names = p.assign(args, "subdivision_name", 2)
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	if not(subdivision_names[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_names[3] = nil --display the third subdivision_type only if suburb or neighborhood
	end
	
	--if say "Voivodeship" is found within the subdivision_type, then specially handle
	--by adding Voivodeship to the end if not already present
	for x, y in ipairs (subdivision_types) do
		local special_types = {
			"Voivodeship"
		}
		for i, j in ipairs(special_types) do
			if subdivision_names[x] and string.find(y, j, 1, true)
				and not string.find(subdivision_names[x], j, 1, true) then
				subdivision_names[x] = subdivision_names[x].." "..j
			end
		end
	end
	
	for x, y in ipairs (subdivision_names) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivision_names[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivision_names
				subdivision_names[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivision_names)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end

	return p.shortdesc(settlement_type..location, frame)..p.categories
end

return p</text>
      <sha1>3c5crspyokscrdg2absr2vdfp0zlk4h</sha1>
    </revision>
    <revision>
      <id>3083857</id>
      <parentid>3083856</parentid>
      <timestamp>2022-01-20T21:10:37Z</timestamp>
      <contributor>
        <username>Pppery</username>
        <id>55</id>
      </contributor>
      <comment>Capitalize description per edit request</comment>
      <origin>3083857</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4399" sha1="jxk9ft269xpljj8n3rp02vakmrxrq6c" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

function p.assign (args, argname, num)
	local val
	local var = {}
	for i = 0,num do
		--handle initial "subdivision_foo" without number
		if i == 0 then
			val = ""
		else
			val = tostring(i)
		end
		var[i+1] = p.validate(plain(args[argname..val]))
	end
	return var
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Pages using infobox settlement with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as extraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivisions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London, England"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivision_types = {}
	local subdivision_names = {}
	local args = getArgs (frame, {parentOnly = true})
	local settlement_type = p.validate(plain(args.settlement_type or args.type), "settlement type") or "Place"
	local short_description = plain(args.short_description)
	subdivision_types = p.assign(args, "subdivision_type", 2)
	subdivision_names = p.assign(args, "subdivision_name", 2)
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	if not(subdivision_names[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_names[3] = nil --display the third subdivision_type only if suburb or neighborhood
	end
	
	--if say "Voivodeship" is found within the subdivision_type, then specially handle
	--by adding Voivodeship to the end if not already present
	for x, y in ipairs (subdivision_types) do
		local special_types = {
			"Voivodeship"
		}
		for i, j in ipairs(special_types) do
			if subdivision_names[x] and string.find(y, j, 1, true)
				and not string.find(subdivision_names[x], j, 1, true) then
				subdivision_names[x] = subdivision_names[x].." "..j
			end
		end
	end
	
	for x, y in ipairs (subdivision_names) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivision_names[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivision_names
				subdivision_names[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivision_names)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end
	
	local language = mw.language.getContentLanguage()
	return p.shortdesc(language:ucfirst(settlement_type..location), frame)..p.categories
end

return p</text>
      <sha1>jxk9ft269xpljj8n3rp02vakmrxrq6c</sha1>
    </revision>
    <revision>
      <id>3083858</id>
      <parentid>3083857</parentid>
      <timestamp>2022-02-12T16:40:17Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jonesey95</username>
        <id>0</id>
      </contributor>
      <comment>capitalize SD when provided within the infobox</comment>
      <origin>3083858</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4417" sha1="t2mz30je7t96kwqks5qhdculsvlavsm" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

function p.assign (args, argname, num)
	local val
	local var = {}
	for i = 0,num do
		--handle initial "subdivision_foo" without number
		if i == 0 then
			val = ""
		else
			val = tostring(i)
		end
		var[i+1] = p.validate(plain(args[argname..val]))
	end
	return var
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Pages using infobox settlement with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as extraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivisions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London, England"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivision_types = {}
	local subdivision_names = {}
	local args = getArgs (frame, {parentOnly = true})
	local settlement_type = p.validate(plain(args.settlement_type or args.type), "settlement type") or "Place"
	local short_description = plain(args.short_description)
	subdivision_types = p.assign(args, "subdivision_type", 2)
	subdivision_names = p.assign(args, "subdivision_name", 2)
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(language:ucfirst(short_description), frame)
		end
	end
	
	if not(subdivision_names[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_names[3] = nil --display the third subdivision_type only if suburb or neighborhood
	end
	
	--if say "Voivodeship" is found within the subdivision_type, then specially handle
	--by adding Voivodeship to the end if not already present
	for x, y in ipairs (subdivision_types) do
		local special_types = {
			"Voivodeship"
		}
		for i, j in ipairs(special_types) do
			if subdivision_names[x] and string.find(y, j, 1, true)
				and not string.find(subdivision_names[x], j, 1, true) then
				subdivision_names[x] = subdivision_names[x].." "..j
			end
		end
	end
	
	for x, y in ipairs (subdivision_names) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivision_names[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivision_names
				subdivision_names[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivision_names)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end
	
	local language = mw.language.getContentLanguage()
	return p.shortdesc(language:ucfirst(settlement_type..location), frame)..p.categories
end

return p</text>
      <sha1>t2mz30je7t96kwqks5qhdculsvlavsm</sha1>
    </revision>
    <revision>
      <id>3083859</id>
      <parentid>3083858</parentid>
      <timestamp>2022-02-12T16:41:20Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jonesey95</username>
        <id>0</id>
      </contributor>
      <comment>add local</comment>
      <origin>3083859</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4470" sha1="sdmmtzi7bcwz6i491hq96btdkaj2hrz" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

function p.assign (args, argname, num)
	local val
	local var = {}
	for i = 0,num do
		--handle initial "subdivision_foo" without number
		if i == 0 then
			val = ""
		else
			val = tostring(i)
		end
		var[i+1] = p.validate(plain(args[argname..val]))
	end
	return var
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Pages using infobox settlement with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as extraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivisions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London, England"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivision_types = {}
	local subdivision_names = {}
	local args = getArgs (frame, {parentOnly = true})
	local settlement_type = p.validate(plain(args.settlement_type or args.type), "settlement type") or "Place"
	local short_description = plain(args.short_description)
	subdivision_types = p.assign(args, "subdivision_type", 2)
	subdivision_names = p.assign(args, "subdivision_name", 2)
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			local language = mw.language.getContentLanguage()
			return p.shortdesc(language:ucfirst(short_description), frame)
		end
	end
	
	if not(subdivision_names[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_names[3] = nil --display the third subdivision_type only if suburb or neighborhood
	end
	
	--if say "Voivodeship" is found within the subdivision_type, then specially handle
	--by adding Voivodeship to the end if not already present
	for x, y in ipairs (subdivision_types) do
		local special_types = {
			"Voivodeship"
		}
		for i, j in ipairs(special_types) do
			if subdivision_names[x] and string.find(y, j, 1, true)
				and not string.find(subdivision_names[x], j, 1, true) then
				subdivision_names[x] = subdivision_names[x].." "..j
			end
		end
	end
	
	for x, y in ipairs (subdivision_names) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivision_names[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivision_names
				subdivision_names[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivision_names)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end
	
	local language = mw.language.getContentLanguage()
	return p.shortdesc(language:ucfirst(settlement_type..location), frame)..p.categories
end

return p</text>
      <sha1>sdmmtzi7bcwz6i491hq96btdkaj2hrz</sha1>
    </revision>
    <revision>
      <id>3083860</id>
      <parentid>3083859</parentid>
      <timestamp>2022-02-12T16:42:26Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jonesey95</username>
        <id>0</id>
      </contributor>
      <comment>rv; I am bad at Lua</comment>
      <origin>3083860</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4399" sha1="jxk9ft269xpljj8n3rp02vakmrxrq6c" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

function p.assign (args, argname, num)
	local val
	local var = {}
	for i = 0,num do
		--handle initial "subdivision_foo" without number
		if i == 0 then
			val = ""
		else
			val = tostring(i)
		end
		var[i+1] = p.validate(plain(args[argname..val]))
	end
	return var
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Pages using infobox settlement with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as extraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivisions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London, England"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivision_types = {}
	local subdivision_names = {}
	local args = getArgs (frame, {parentOnly = true})
	local settlement_type = p.validate(plain(args.settlement_type or args.type), "settlement type") or "Place"
	local short_description = plain(args.short_description)
	subdivision_types = p.assign(args, "subdivision_type", 2)
	subdivision_names = p.assign(args, "subdivision_name", 2)
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			return p.shortdesc(short_description, frame)
		end
	end
	
	if not(subdivision_names[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_names[3] = nil --display the third subdivision_type only if suburb or neighborhood
	end
	
	--if say "Voivodeship" is found within the subdivision_type, then specially handle
	--by adding Voivodeship to the end if not already present
	for x, y in ipairs (subdivision_types) do
		local special_types = {
			"Voivodeship"
		}
		for i, j in ipairs(special_types) do
			if subdivision_names[x] and string.find(y, j, 1, true)
				and not string.find(subdivision_names[x], j, 1, true) then
				subdivision_names[x] = subdivision_names[x].." "..j
			end
		end
	end
	
	for x, y in ipairs (subdivision_names) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivision_names[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivision_names
				subdivision_names[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivision_names)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end
	
	local language = mw.language.getContentLanguage()
	return p.shortdesc(language:ucfirst(settlement_type..location), frame)..p.categories
end

return p</text>
      <sha1>jxk9ft269xpljj8n3rp02vakmrxrq6c</sha1>
    </revision>
    <revision>
      <id>3083861</id>
      <parentid>3083860</parentid>
      <timestamp>2022-02-12T16:43:02Z</timestamp>
      <contributor>
        <username>wikipedia&gt;Jonesey95</username>
        <id>0</id>
      </contributor>
      <comment>Undid revision 1071438822 by [[Special:Contributions/Jonesey95|Jonesey95]] ([[User talk:Jonesey95|talk]]). false positive?</comment>
      <origin>3083861</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4470" sha1="sdmmtzi7bcwz6i491hq96btdkaj2hrz" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

function p.assign (args, argname, num)
	local val
	local var = {}
	for i = 0,num do
		--handle initial "subdivision_foo" without number
		if i == 0 then
			val = ""
		else
			val = tostring(i)
		end
		var[i+1] = p.validate(plain(args[argname..val]))
	end
	return var
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Pages using infobox settlement with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as extraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivisions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London, England"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivision_types = {}
	local subdivision_names = {}
	local args = getArgs (frame, {parentOnly = true})
	local settlement_type = p.validate(plain(args.settlement_type or args.type), "settlement type") or "Place"
	local short_description = plain(args.short_description)
	subdivision_types = p.assign(args, "subdivision_type", 2)
	subdivision_names = p.assign(args, "subdivision_name", 2)
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			local language = mw.language.getContentLanguage()
			return p.shortdesc(language:ucfirst(short_description), frame)
		end
	end
	
	if not(subdivision_names[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_names[3] = nil --display the third subdivision_type only if suburb or neighborhood
	end
	
	--if say "Voivodeship" is found within the subdivision_type, then specially handle
	--by adding Voivodeship to the end if not already present
	for x, y in ipairs (subdivision_types) do
		local special_types = {
			"Voivodeship"
		}
		for i, j in ipairs(special_types) do
			if subdivision_names[x] and string.find(y, j, 1, true)
				and not string.find(subdivision_names[x], j, 1, true) then
				subdivision_names[x] = subdivision_names[x].." "..j
			end
		end
	end
	
	for x, y in ipairs (subdivision_names) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivision_names[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivision_names
				subdivision_names[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivision_names)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end
	
	local language = mw.language.getContentLanguage()
	return p.shortdesc(language:ucfirst(settlement_type..location), frame)..p.categories
end

return p</text>
      <sha1>sdmmtzi7bcwz6i491hq96btdkaj2hrz</sha1>
    </revision>
    <revision>
      <id>3083862</id>
      <parentid>3083861</parentid>
      <timestamp>2025-12-08T02:08:29Z</timestamp>
      <contributor>
        <username>Immanuelle</username>
        <id>3</id>
      </contributor>
      <minor/>
      <comment>58 revisions imported from [[:wikipedia:Module:Settlement_short_description]]</comment>
      <origin>3083861</origin>
      <model>Scribunto</model>
      <format>text/plain</format>
      <text bytes="4470" sha1="sdmmtzi7bcwz6i491hq96btdkaj2hrz" xml:space="preserve">--generates auto short description for use in infobox settlement
local p = {}
p.categories = ""
local plain = require('Module:Plain text')._main
local getArgs = require('Module:Arguments').getArgs
local tableTools = require ('Module:TableTools')

function p.reverseTable (init)
	init[1], init[3] = init[3], init[1]
	return init
end

function p.assign (args, argname, num)
	local val
	local var = {}
	for i = 0,num do
		--handle initial "subdivision_foo" without number
		if i == 0 then
			val = ""
		else
			val = tostring(i)
		end
		var[i+1] = p.validate(plain(args[argname..val]))
	end
	return var
end

--Display short description using {{short description}}
function p.shortdesc(text, frame)
	return frame:expandTemplate{title = 'Short description', args = {text, 'noreplace'}}
end

function p.category (cattype)
	local category = string.format('[[Category:Pages using infobox settlement with bad %s]]', cattype)
	if category then p.categories = p.categories..category end --categorize
end

--sanity and other checks
function p.validate (parameter, cat)
	if not parameter then return nil end
	parameter = parameter:gsub('%b()', '') --remove things in brackets as extraneous information
			   :gsub('%s+', ' ') --fix possible extra spaces from previous cleanup
			   :gsub('^%s+', '') --trim spaces from beginning
			   :gsub('%s+$', '') --trim spaces from end
	if parameter:match("[,;]") or not parameter:match("%a") then --must have some letters, ignore if multiple types/subdivisions
		if cat then p.category (cat) end
		return nil
	end
	if (parameter == "") then return nil end
	return parameter
end

--removes redundancy like "England, United Kingdom" and fixes issues like "Foo in United States" (to "Foo in the United States")
--also used in Module:Type in location
function p.cleanupLoc (location)
	if location == "" then return nil end
	local replacements = {
		["England, United Kingdom"] =  "England",
		["Scotland, United Kingdom"] =  "Scotland",
		["Wales, United Kingdom"] =  "Wales",
		["New York City, New York, United States"] =  "New York City",
		["^United States$"] = "the United States",
		["London, United Kingdom"] = "London, England"
	}
	for i, v in pairs(replacements) do 
		location = location:gsub(i, v) --series of replacements
	end
	return location
end

function p.main(frame)
	local categories = ""
	local subdivision_types = {}
	local subdivision_names = {}
	local args = getArgs (frame, {parentOnly = true})
	local settlement_type = p.validate(plain(args.settlement_type or args.type), "settlement type") or "Place"
	local short_description = plain(args.short_description)
	subdivision_types = p.assign(args, "subdivision_type", 2)
	subdivision_names = p.assign(args, "subdivision_name", 2)
	
	if short_description then
		if (short_description == 'no') then
			return
		else
			local language = mw.language.getContentLanguage()
			return p.shortdesc(language:ucfirst(short_description), frame)
		end
	end
	
	if not(subdivision_names[3] and
		(string.find(settlement_type, '[nN]eighbo[u]?rhood') or string.find(settlement_type, '[sS]uburb'))) then
		subdivision_names[3] = nil --display the third subdivision_type only if suburb or neighborhood
	end
	
	--if say "Voivodeship" is found within the subdivision_type, then specially handle
	--by adding Voivodeship to the end if not already present
	for x, y in ipairs (subdivision_types) do
		local special_types = {
			"Voivodeship"
		}
		for i, j in ipairs(special_types) do
			if subdivision_names[x] and string.find(y, j, 1, true)
				and not string.find(subdivision_names[x], j, 1, true) then
				subdivision_names[x] = subdivision_names[x].." "..j
			end
		end
	end
	
	for x, y in ipairs (subdivision_names) do
		if y then
			if string.find(settlement_type, y, 1, true) then --if the subdivision is found within the settlement type
				subdivision_names[x] = nil --don't display redundancy
				p.category ("settlement type")
			end
			if y == mw.title.getCurrentTitle().text then --if the title is the same as one of the subdivision_names
				subdivision_names[x] = nil --don't display redundancy
			end
		end
	end

	local location = table.concat(tableTools.compressSparseArray(p.reverseTable(subdivision_names)), ', ')
	
	location = p.cleanupLoc (location)
	
	if location then location =  " in " .. location else location = "" end
	
	local language = mw.language.getContentLanguage()
	return p.shortdesc(language:ucfirst(settlement_type..location), frame)..p.categories
end

return p</text>
      <sha1>sdmmtzi7bcwz6i491hq96btdkaj2hrz</sha1>
    </revision>
  </page>
</mediawiki>
